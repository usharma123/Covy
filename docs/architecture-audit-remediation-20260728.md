# Architecture audit remediation ledger

This is the observation-level completion contract for the Packet28 architecture
audit. It deliberately separates immutable audit facts from later source state,
implementation, evidence, and integration status.

## Traceability boundary

- **Audit artifact:**
  [`docs/audits/architecture-review-20260728-114543.html`](audits/architecture-review-20260728-114543.html)
- **Audit artifact SHA-256:** `126de8fc65b42bf5000ad1744293a1507e4abf18c1cbabc57dbb9e1a217195a9`
- **Audit artifact size:** 741 lines, 64,714 bytes
- **Audit date:** 28 July 2026
- **Provisional current-source snapshot used by this revision:** commit
  `1d7b98137e97ba670e1c26f5e3b428ce9d912928`, tree
  `7b774937f5a5e1389e52699f9df691071f7c8d67`
- **Snapshot policy:** current-source statements below describe that clean
  committed tree only. Live dirty-worktree changes are not attributed to this
  snapshot or to a closing commit. The integration owner must run the final
  ledger synchronization and validator again after the later broker, daemon
  documentation, README, evidence-recapture, and final-gate commits land.

Source keys use the immutable HTML line numbers, for example
`#daemon-protocol L67–119`. Baseline cells reproduce or tightly paraphrase what
the audit said; they are never rewritten to match later source. A row can share
implementation with another row, but it cannot disappear into another row.

Current validation cells begin with `HISTORICAL`, `CONFIRMED CURRENT`,
`ALREADY FIXED`, `PARTIALLY FIXED`, `NOT REPRODUCIBLE`, `PRESERVED`, or
`REVALIDATION REQUIRED`; a short qualifier may follow without changing the
class. Status values are:

- `DONE`: the row-specific invariant and focused evidence are committed at the
  named snapshot;
- `PARTIAL`: useful implementation or evidence landed, but the row's acceptance
  criteria are not fully met;
- `PENDING`: the committed snapshot still exhibits the finding or lacks its
  required evidence;
- `REGRESSED`: a prior fix or generated invariant is not true at the snapshot;
- `EVIDENCE ONLY`: provenance, measurement, or claim-boundary row;
- `NOT REPRODUCIBLE`: the source claim did not survive controlled revalidation;
- `PRESERVE`: verified strength that must survive remediation;
- `REFERENCE`: external validation material, not a code closure.

The canonical final gate is intentionally separate from row-focused evidence.
A row marked `DONE` can still be blocked from release by an unfilled or failing
final gate.

<!-- BEGIN: SOURCE-DERIVED-AUDIT-ROWS -->
## Provenance, verdict, and audit-time qualifications

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| PROV-01 | Header L45–55 | The generated report is the 28 July 2026 Packet28 deep Rust architecture audit, updated with task `019fa949-eafe-7aa0-bff3-38d4837a9f83`. | HISTORICAL | Preserve the exact artifact identity, digest, date, and addendum task identity. | Artifact digest and source coordinates remain recorded; later reports use a new identity. | SHA-256 and byte/line counts are recorded above. | Documentation-only trace row | EVIDENCE ONLY |
| PROV-02 | Header L48–51 | Baseline inventory was 28 crates, 543 Rust files, and approximately 154K lines. | HISTORICAL | Never promote baseline inventory to a current repository count. | Baseline values remain verbatim; current values are generated independently. | At the provisional snapshot the README generator reports 32 crates, 643 Rust files, and 227,205 lines, demonstrating why the baseline is immutable. | Audit artifact; current check is uncommitted evidence | EVIDENCE ONLY |
| PROV-03A | `#audit` L533–545 | `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` passed at audit time. | HISTORICAL | Preserve the exact command/result as audit-time evidence. | No later run is backdated into this row. | Verification table records `Pass`. | Audit artifact | EVIDENCE ONLY |
| PROV-03B | `#audit` L533–545 | `cargo test --workspace --all-features --locked` passed before a later concurrent edit, and the run executed zero doctests. | HISTORICAL | Preserve the `Pass*` timing qualification and zero-doctest fact. | Later doctests/tests use new evidence rows and do not rewrite this result. | Verification table and asterisk explanation retain the boundary. | Audit artifact | EVIDENCE ONLY |
| PROV-03C | `#audit` L533–545 | Strict broken-link rustdoc failed on `testy-cli-common/impact.rs:121` and `covy-cli/cmd_map_paths.rs:27`. | HISTORICAL | Preserve exact baseline failure locations separately from their later fix. | DOC-01 records closure without changing this audit-time result. | Verification table records `Fail` and both paths. | Audit artifact; fixed by `3db342d` | EVIDENCE ONLY |
| PROV-03D | `#audit` L533–545 | `-W missing_docs` produced 4,439 warnings; only one library root had crate docs, with no doctest examples or Errors/Panics sections. | HISTORICAL | Preserve exact baseline count and breadth qualification. | DOC/TST rows track later family-specific work without claiming the baseline count is current. | Verification table records `4,439 warnings` and its explanation. | Audit artifact | EVIDENCE ONLY |
| PROV-03E | `#audit` L533–545 | Focused panic/unsafe/clone Clippy groups reported 66 / 17 / ~60 and were not enforced by workspace policy. | HISTORICAL | Preserve each audit-time count and non-enforcement boundary. | REP-05B/C/D and PER-09 record separate current validation and closure. | Verification table records all three values. | Audit artifact | EVIDENCE ONLY |
| PROV-03F | `#audit` L533–545 | Post-edit formatting and context-kernel prefix tests failed after a user-owned file changed at 11:41; the audit did not modify it. | HISTORICAL | Preserve `Fail*`, timestamp, ownership, and read-only qualification. | No remediation commit or later result is attributed to the audit. | Verification table and asterisk explanation retain the qualification. | Audit artifact | EVIDENCE ONLY |
| PROV-04 | Footer L735–737 | The audit was read-only and did not modify repository files. | HISTORICAL | Audit authorship must not be attributed to repository changes. | Ledger and commit history distinguish review observations from remediation commits. | Footer states no repository files were modified. | Audit artifact | EVIDENCE ONLY |
| PROV-05A | `#thread-addendum` L577–581 | In the addendum, “Confirmed” described source mechanics. | HISTORICAL | Source-mechanics validation is not treated as runtime measurement or current closure. | Every confirmed baseline mechanism has a separate current-source field. | Source definition retained verbatim in meaning. | Audit artifact | EVIDENCE ONLY |
| PROV-05B | `#thread-addendum` L577–581 | “Live snapshot” described volatile local state. | HISTORICAL | Live measurements always retain scope/time and are not product constants. | PER-11A/B/C preserve scope and sampling time. | Source definition and sample rows. | Audit artifact | EVIDENCE ONLY |
| PROV-05C | `#thread-addendum` L577–581 | “Historical benchmark” meant the benchmark was not rerun against current HEAD. | HISTORICAL | Historical and current controlled benchmarks remain separate evidence records. | PER-10 retains both identities without cross-environment ratio claims. | Source definition and benchmark provenance. | Audit artifact | EVIDENCE ONLY |
| PROV-06 | `#thread-addendum` L585–588, L646 | Live task/store values were sampled at 2026-07-28 11:55 EDT and could change while the daemon ran. | HISTORICAL | Timestamp and scope accompany every copied live value. | No live number is described as a product constant or current snapshot. | PER-11A/B/C record the three independently scoped samples. | Audit artifact | EVIDENCE ONLY |
| PROV-07 | `#thread-addendum` L588, L646 | The 10,375.754 ms workspace-index result was measured 31 March 2026 with p28 `59e54fb` / packet28d `0.2.39`. | HISTORICAL | Historical benchmark provenance is preserved separately from current controlled runs. | Commit/version/date remain attached; no cross-environment improvement ratio is inferred. | PER-10 and the isolated current-source harness retain this boundary. | `aee9028` for later current-source evidence | EVIDENCE ONLY |
| PROV-08A | Footer L735–737 | The requested architecture skill was used; its referenced codebase-design skill was unavailable, so embedded vocabulary and deletion tests were used. | HISTORICAL | Preserve the method/fallback without claiming the unavailable skill ran. | Any reproduction records the same limitation or names a newly available method separately. | Exact footer qualification retained. | Audit artifact | EVIDENCE ONLY |
| PROV-08B | Footer L735–737 | The addendum was appended after current-source validation and only the generated report changed. | HISTORICAL | Addendum authorship is confined to the report artifact. | No repository remediation is attributed to the addendum task. | Exact footer qualification retained. | Audit artifact | EVIDENCE ONLY |
| SUM-01 | Header L52–55; `#audit` L481–490 | Verdict: strong direction, broad tests, and an acyclic graph, but not yet best-practice because reproducibility, MSRV, public docs, lifecycle correctness, and file-only seams needed material work. | HISTORICAL | Treat the verdict as a baseline synthesis, not an independently closable defect. | Component rows close independently; a new audit is required to issue a new verdict. | Related rows: INV-01/03, REP-01/02/03, DOC-01/02, COR-03…07, ARC-01…06. | Audit artifact | EVIDENCE ONLY |
| SUM-02 | `#audit` L494–500 | Linux ABI, fail-open policy, cancellation, forced-TCP shutdown, and macOS cleanup were “fix before more structural refactoring” findings. | HISTORICAL | Preserve the baseline priority even after fixes land. | Each named correctness observation retains a separate COR row and commit trail. | COR-01A/B through COR-05A/B provide the one-to-one trace. | Multiple; see COR rows | EVIDENCE ONLY |

## Baseline scorecard

Scorecard rows are immutable summaries. They are not silently upgraded when
individual remediation rows close.

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| SCORE-01 | `#audit` L481–490 | Crate graph: **B**, 75%; acyclic with two inverted seams. | HISTORICAL | Preserve rating and link it to graph/seam rows. | A future rating requires a new review. | INV-01, ARC-01, and ARC-02 are the underlying rows. | Audit artifact | EVIDENCE ONLY |
| SCORE-02 | `#audit` L481–490 | Rust idioms: **B**, 72%; safe core with FFI debt. | HISTORICAL | Preserve rating and its safe-core/FFI rationale. | A future rating requires a new review. | INV-04A/05 and COR-01A/B are the underlying rows. | Audit artifact | EVIDENCE ONLY |
| SCORE-03 | `#audit` L481–490 | Testing: **B+**, 82%; broad coverage with a costly harness. | HISTORICAL | Preserve rating and both positive/negative qualifications. | A future rating requires a new review. | INV-03 and TST-08A/B are the underlying rows. | Audit artifact | EVIDENCE ONLY |
| SCORE-04 | `#audit` L481–490 | Rustdoc: **D**, 30%; broken links and no examples. | HISTORICAL | Preserve rating. | A future rating requires a new review. | DOC-01, DOC-02A…D, and TST-07A/B trace the reasons. | Audit artifact | EVIDENCE ONLY |
| SCORE-05 | `#audit` L481–490 | Lifecycle: **C+**, 56%; cancellation and shutdown gaps. | HISTORICAL | Preserve rating. | A future rating requires a new review. | COR-03…07 and ASY-02A/B trace the reasons. | Audit artifact | EVIDENCE ONLY |
| SCORE-06 | `#audit` L481–490 | Errors: **C+**, 55%; typed pockets with erased seams. | HISTORICAL | Preserve rating. | A future rating requires a new review. | API-01A…E trace independently closable library seams. | Audit artifact | EVIDENCE ONLY |
| SCORE-07 | `#audit` L481–490 | CI/reproducibility: **C**, 48%; lockfile and MSRV were not enforced. | HISTORICAL | Preserve rating. | A future rating requires a new review. | REP-01…10 trace the reasons. | Audit artifact | EVIDENCE ONLY |
| SCORE-08 | `#audit` L481–490 | Scalability: **C**, 45%; incremental paths rewrote global state. | HISTORICAL | Preserve rating. | A future rating requires a new review. | PER-01…06 trace the reasons. | Audit artifact | EVIDENCE ONLY |
| SCORE-09 | `#audit` L481–490 | Overall: **B-**, 65%; healthy, not best-in-class. | HISTORICAL | Preserve rating as the audit’s overall synthesis. | Only a new audit can replace it. | SUM-01 records the corresponding verdict. | Audit artifact | EVIDENCE ONLY |

## Correctness and lifecycle

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| COR-01A | `#audit` L494–500 | Linux preload hooks exported fixed-arity `open`/`openat` functions and cast variadic libc symbols to fixed function pointers; the in-repo macOS bridge correctly used C variadics. | ALREADY FIXED | C owns every variadic declaration, argument read, and `RTLD_NEXT`; fixed Rust callbacks only perform replacement lookup. | All four Linux symbols work at both arities, preserve modes/replacement paths/`errno`, expose no fixed Rust variadic ABI, and retain the macOS bridge behavior. | Linux `LD_PRELOAD` C-caller integration coverage plus platform-neutral/macOS package checks. | `0f708f1` | DONE |
| COR-01B | `#audit` L518–528 | Linux public unsafe hooks lacked Safety docs; focused unsafe inventory found 17 undocumented blocks. | ALREADY FIXED | Public unsafe contracts and peripheral unsafe blocks are documented and bounded by policy. | Strict selected lint/source inventory rejects undocumented expansion and core algorithms remain safe. | FFI safety documentation plus unsafe/panic policy inventory. | `0f708f1`, `571a53f` | DONE |
| COR-02 | `#audit` L494–500 | Fifteen command paths converted malformed or unreadable configuration to defaults, including coverage gates. | ALREADY FIXED | Absence is distinct from path-aware read/parse failure; present-invalid configuration propagates. | No production erased-load pattern or existence-check race remains; all affected commands fail closed. | Foundation and affected CLI tests; source verifier for erased loads. | `803a8e9` | DONE |
| COR-03 | `#audit` L494–500 | `TaskCancel` deleted bookkeeping without stopping sequence work or child processes; completion could recreate the removed task. | ALREADY FIXED | Cancellation quiesces the owned generation, reaps children, preserves terminal `Cancelled` history, rejects stale completion, and is idempotent. | Before-start, in-flight, child, race, repeat, and same-ID-reactivation cases show no resurrection or stale mutation. | Focused cancellation/lifecycle regressions and packet28d suite introduced with the owned-generation implementation. | `fe5061f`; typed-state support `a8f3d4e`, `e5cd23e` | DONE |
| COR-04 | `#audit` L494–500 | Forced-TCP `Stop` acknowledged without waking the TCP accept loop. | ALREADY FIXED | One structured transport lifecycle signal wakes and joins both TCP and Unix listeners. | Forced-TCP and Unix stop exit within a bound and release their endpoints. | Tokio transport shutdown and forced-TCP/Unix lifecycle tests. | `8ea0492` | DONE |
| COR-05A | `#audit` L494–500 | macOS instruction swap could bypass restoration and child cleanup on post-spawn errors or unwinding. | ALREADY FIXED | A guard owns journal, staged files, relay, child, and restoration on success, error, timeout, and drop. | Injected failure/unwind paths restore or retain authenticated recovery material and reap children. | Swap fault-injection, signal shutdown, recovery, and cleanup tests. | `25aa04e`, `dc88e9c`, `909f2b8` | DONE |
| COR-05B | `#audit` L494–500 | Recovery could delete the current file before verifying its backup. | ALREADY FIXED | Recovery authenticates and stages the backup before any destructive replacement. | Missing, corrupt, or unauthenticated recovery artifacts never destroy the current file. | Recovery-artifact authentication and crash-window tests. | `25aa04e`, `909f2b8` | DONE |
| COR-06A | `#audit` L518–528 | A partial search-index record could be accepted as clean EOF. | ALREADY FIXED | Clean EOF is recognized only at a record boundary; truncation is corruption. | Every record truncation boundary fails without publishing a partial layer. | Generated truncation/corruption cases and no-publication checks. | `7ad5adf`; durable retry follow-up `32c82da` | DONE |
| COR-06B | `#audit` L518–528 | Trailing partial lookup rows and invalid posting ranges could become clean misses. | ALREADY FIXED | Lookup alignment, lengths, ranges, and arithmetic are validated before publication/query. | Partial rows, overflow, and out-of-range postings fail with provenance; valid EOF remains valid. | Search integrity/property cases and authenticated-generation checks. | `7ad5adf`, `a24202b`, `6a49f03` | DONE |
| COR-07A | `#audit` L518–528 | Subscriber and watch queues were unbounded. | ALREADY FIXED | Subscriber/watch channels are bounded with explicit lag/replay behavior. | Slow consumers cannot cause unbounded memory growth; overflow is observable and recoverable. | Bounded-channel, lag, replay, and shutdown tests in async daemon transport. | `8ea0492` | DONE |
| COR-07B | `#audit` L518–528 | Thread-per-connection servers had no read deadlines or structured connection ownership. | ALREADY FIXED | Connections have bounded ownership, deadlines, caps, and joined shutdown for TCP and Unix. | Stalled header/body, non-reading writer, cap, parity, and stop cases remain bounded. | Structured transport lifecycle and stalled-client tests. | `8ea0492`, architecture boundary `0e3aa17` | DONE |
| COR-08A | `#audit` L518–528 | Task lifecycle used parallel booleans that admitted invalid combinations. | ALREADY FIXED | Runtime enums and checked transitions model task lifecycle while preserving wire compatibility. | Invalid combinations are unrepresentable internally; all legal transitions and legacy wires are covered. | Exhaustive lifecycle transition and compatibility/compile-fail tests. | `a8f3d4e`, `e5cd23e` | DONE |
| COR-08B | `#audit` L518–528 | Search status was a string plus optional fields. | ALREADY FIXED | Typed search/index state owns legal state-dependent fields and transitions. | Invalid state/field combinations cannot enter published state; compatibility behavior is preserved. | State transition, serialization, and corruption/retry tests. | `a8f3d4e`, `32c82da` | DONE |

## Crate and module architecture

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| ARC-01A | `#daemon-protocol` L67–119 | Rated **Strong · ports & adapters**: daemon wire DTO/framing depended on kernel, memory, and reducer implementations. | ALREADY FIXED | Wire DTOs/framing live in implementation-free packet/protocol crates; storage/trust/execution stay in runtime crates. | Protocol crates have no prohibited implementation dependencies and preserve serialized compatibility. | Protocol compatibility, framing, malformed-input, JSON golden, no-default-feature, and dependency tests. | `e56d3f0`, `b3ac32f` | DONE |
| ARC-01B | `#daemon-protocol` L67–119 | Small clients and adapters crossed the daemon implementation seam. | ALREADY FIXED | Shim, p28, daemon, and adapters depend on protocol contracts rather than daemon implementation. | Metadata/source architecture checks reject implementation edges from every protocol-only consumer. | Direct consumer migration plus permanent negative-dependency guard. | `c7e6470`, `73c535a` | DONE |
| ARC-01C | `#daemon-protocol` L67–119 | Compatibility re-exports risked recreating the wide implementation surface. | ALREADY FIXED | Compatibility is explicit and quarantined behind a frozen facade. | Supported old imports compile; new prohibited dependencies fail architecture checks. | Compatibility compile tests and facade/architecture invariants. | `0b93f5b`, `5389ce5` | DONE |
| ARC-02A | `#kernel-composition` L121–171 | Rated **Strong · in-process**: kernel mechanism owned 15 concrete built-ins. | ALREADY FIXED | Kernel retains scheduling, policy, cache, audit, and execution mechanism; composition owns built-in registration. | Kernel metadata/source has no concrete reducer dependencies; registry behavior and routing remain equivalent. | The concrete-free mechanism, builtins composition, and 45-name compatibility facade pass dependency, routing, compatibility, cancellation, and public-surface inventory tests. | `95f404b`, `0888948`, `27812ed`, `c9060ca`; inventory `1d7b981` | DONE |
| ARC-02B | `#kernel-composition` L121–171 | The concrete dependencies included a CLI adapter, leaking composition into the kernel. | ALREADY FIXED | CLI adapter ownership moves to an application/composition boundary. | A negative-dependency rule rejects kernel-to-CLI edges and end-to-end routing remains compatible. | `context-kernel-mechanism` has no concrete reducer or CLI dependency; the architecture mutation tests enforce that boundary while builtins preserve all 16 routes. | `95f404b`, `0888948`, `c9060ca` | DONE |
| ARC-03 | `#mcp-lifecycle` L173–220 | Rated **Strong · in-process**: MCP catalog, parsing, dispatch, execution, summary, and response shape were mechanically coupled but scattered across shallow modules. | ALREADY FIXED | Each native tool family owns schema/parse/execute/summary/result shaping behind session/daemon seams. | A tool-family change is localized; tool-list and structural output remain compatible. | Native lifecycle colocation plus reviewed lifecycle snapshot/compatibility tests. | `ee0b556` | DONE |
| ARC-04A | `#broker-module` L222–273 | Rated **Strong · in-process**: broker sibling files imported the parent prelude wholesale and shared a hidden interface. | ALREADY FIXED at `b69c8bab` | `packet28d::broker` owns private child modules under `src/broker/` and exposes only explicit crate-internal facade ports; broker children use named imports and cannot depend on the application lifecycle. | The source guard rejects wildcard imports, visible child modules, legacy sibling files, application dependencies, and facade bypasses; broker task-status dispatch is JSON-equivalent to calling the owning facade directly. | 29 architecture mutation tests; live architecture invariant; exact broker dispatch parity test; strict all-target/all-feature packet28d Clippy. | `63afae08`, `c8a34314` | DONE |
| ARC-04B | `#broker-module` L222–273 | `packet28d` main mixed process bootstrap with broker ownership. | ALREADY FIXED at `b69c8bab` | `packet28d` main now owns only CLI parsing, error presentation, and exit status while `packet28d::serve` owns recovery, construction, orchestration, and cleanup in `application.rs`. | The architecture guard enforces the entrypoint line budget, delegation seam, and absence of runtime internals; a process test proves the binary preserves the library startup error and exit-code contract. | 29 architecture mutation tests; `bootstrap_delegation` 1/1; strict all-target/all-feature packet28d Clippy. | `63afae08`, `c8a34314` | DONE |
| ARC-05A | `#memory-ownership` L275–321 | Rated **Strong · local-substitutable**: local memory was a broad re-export hub exposing persistence details. | ALREADY FIXED | A local-memory owner presents a narrow workflow interface; persistence details remain internal. | Supported CLI/MCP behavior compiles while broad implementation exports are absent or quarantined. | Memory ownership refactor and compatibility tests. | `9c04e11` | DONE |
| ARC-05B | `#memory-ownership` L275–321 | Operations independently derived, opened, and initialized environment-derived SQLite storage. | ALREADY FIXED | One store boundary owns path, connection lifecycle, migrations, prepared work, and workflow transactions. | Connection/migration counts are bounded; graph batches share transactions and rollback atomically. | Migration/transaction/workflow tests and streaming parameter follow-up. | `9c04e11`, `8c1f770` | DONE |
| ARC-06 | `#runtime-integration` L324–365 | Rated **Worth exploring · ports & adapters**: runtime modules owned paths while detection, capabilities, setup, and status leaked across repeated `RuntimeKind` switchboards for 12 runtimes. | ALREADY FIXED | Each runtime adapter owns its paths, markers, capabilities, setup actions, and status; setup retains orchestration only. | Catalog order/uniqueness and behavior parity hold; source guard finds no setup switchboard/literal leakage. | 247 suite-cli library tests, 15 setup E2Es, catalog/source invariants, strict package checks. | `91b3ed5` | DONE |
| ARC-07 | `#ground-truth` L704–719; `#roadmap` L724–731 | Public compatibility/re-export modules should be narrowed and quarantined rather than expose implementation details. | ALREADY FIXED for audited daemon/packet seams | Compatibility modules are explicit, documented, and mechanically prevented from widening dependency direction. | Supported surface snapshots/compile tests pass and architecture rules reject implementation leakage. | Packet compatibility exports and daemon facade are explicit and frozen. | `0b93f5b`, `5389ce5` | DONE |
| ARC-08 | `#audit` L563–572; `#ground-truth` L704–719 | Architecture diagrams named layers but no metadata test enforced negative dependency rules. | ALREADY FIXED | Canonical gate executes architecture tests for prohibited crate/module/runtime edges. | Synthetic or fixture violations fail; current metadata/source passes. | Daemon dependency guard and async-runtime boundary architecture tests. | `73c535a`, `0e3aa17` | DONE |

## Reproducibility, CI, release, and tooling

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| REP-01 | `#audit` L551–560 | `Cargo.lock` existed locally but was ignored and untracked, so clean CI/releases resolved fresh graphs. | ALREADY FIXED | The lockfile is tracked, not ignored, and deterministic Cargo gates use `--locked`. | Policy verifier rejects missing/ignored/untracked lockfiles; locked metadata/check/tests succeed. | `verify_workspace_policy.sh` passes in the clean snapshot; CI and release workflows enforce the locked graph. | `018b74a`, `88777d4`, `66d37f2` | DONE |
| REP-02 | `#audit` L551–560 | Declared Rust 1.75 was not credible with selected dependencies requiring at least 1.85; CI tested moving stable only. | ALREADY FIXED | Workspace declares one supportable MSRV and tests that exact toolchain. | Every member inherits the workspace rust-version; exact-version locked check runs in canonical CI. | Workspace declares 1.88.0; policy verifier passes for all 30 members; pinned-toolchain tasks exist. | `018b74a`, `66d37f2`, `d528487` | DONE |
| REP-03A | `#audit` L551–560; `#roadmap` L724–731 | PR CI lacked a complete workspace check/build path. | ALREADY FIXED | One canonical task owns workspace check/build and is invoked by CI. | Local and workflow entrypoints resolve to the same locked commands; drift is mechanically rejected. | Canonical task/verifier and workflow inventory landed. | `66d37f2`, `d528487` | DONE |
| REP-03B | `#audit` L533–560 | Strict Clippy passed locally but PR CI did not enforce it. | ALREADY FIXED | Canonical CI runs workspace/all-target/all-feature Clippy with warnings denied. | A synthetic warning fails the gate; policy is not weakened to obtain green. | Canonical gate and workspace lint policy include strict Clippy. | `018b74a`, `580954c`, `66d37f2` | DONE |
| REP-03C | `#audit` L533–560 | Strict rustdoc failed locally and PR CI did not execute it. | ALREADY FIXED | Canonical CI runs strict workspace rustdoc and doctests. | Broken links/invalid HTML fail; documented doctests execute rather than report zero. | Strict rustdoc gate landed; remaining documentation breadth is tracked under DOC/TST rows. | `3db342d`, `66d37f2`, `d528487` | DONE |
| REP-03D | `#audit` L551–560 | CI tested moving stable rather than the declared minimum toolchain. | ALREADY FIXED | Exact MSRV is pinned and exercised separately from stable/current. | Workflow/tooling fails if declared and executed MSRV diverge. | Pinned toolchain and canonical MSRV task. | `018b74a`, `66d37f2`, `d528487` | DONE |
| REP-04 | `#audit` L551–560 | `deny.toml` had useful policy but no workflow executed it. | ALREADY FIXED | Canonical local/CI gate executes supply-chain policy without weakening it. | `cargo deny` policy runs on the locked graph and failures block the gate. | Canonical reproducible release gate includes deny policy. | `66d37f2`, `d528487` | DONE |
| REP-05A | `#audit` L551–560 | Workspace lint inheritance was absent. | ALREADY FIXED | Conservative workspace Rust/Clippy tables exist and every member explicitly inherits them. | Mechanical verifier rejects any non-inheriting member. | `verify_workspace_policy.sh` passes for 32 members at the provisional snapshot. | `018b74a`, `580954c` | DONE |
| REP-05B | `#audit` L533–545 | Focused panic linting found 66 sites and was not workspace policy. | ALREADY FIXED for the selected policy | Panic policy is explicit; production parser/runtime panic paths are repaired or mechanically justified. | Policy verifier and strict Clippy reject prohibited production panic paths; exceptions are narrow and documented. | Safety/panic policy landed; env-parser production panic path was removed at current HEAD. | `571a53f`, `6cbe671` | DONE |
| REP-05C | `#audit` L518–545 | Focused unsafe linting found 17 undocumented blocks; Linux public unsafe hooks lacked Safety docs. | ALREADY FIXED | Unsafe remains peripheral, contracts are documented, and policy prevents undocumented expansion. | Unsafe inventory is mechanically bounded; FFI/platform code documents contracts; core algorithms remain safe. | Variadic FFI fix plus safety-policy/source inventory. | `0f708f1`, `571a53f` | DONE |
| REP-05D | `#audit` L518–545 | Roughly 60 redundant-clone warnings existed and focused clone/performance lints were not policy. | PARTIALLY FIXED | Selected lints inventory candidates; only measured hot-path changes land and rejected changes retain evidence. | Current inventory and benchmark decisions are versioned; no blanket “all clones fixed” claim is made. | PER-09 harness records 65 redundant clones and selected measured removals; many candidates remain intentionally unchanged. | `4635b6c`, `932c4b4`, `3237e6b`, `b736639`, `969cc75` | PARTIAL |
| REP-06 | `#audit` L551–560 | Ninety-three internal path/version declarations repeated workspace topology. | ALREADY FIXED | Internal dependencies have one workspace source and members inherit it without member-local paths. | Metadata/policy verifier finds zero member-local internal paths and preserves an acyclic graph. | `verify_workspace_policy.sh` passes for 30 members and 21 centralized internal dependencies; the baseline count 93 remains historical. | `018b74a` | DONE |
| REP-07 | `#audit` L551–560 | Release tags were not asserted against Cargo/package versions. | ALREADY FIXED | Release preflight validates tag, Cargo, npm/platform-package, and staged artifact versions before publication. | Mismatch fixtures fail; staged packages are built and smoked without publication. | Version/tag preflight and offline staged-package smoke cover binaries, headers, modes, npm pack/publish layout, and arm64 execution. | `66d37f2`, `66ab4ec`; publication-policy follow-up `937876a` | DONE |
| REP-08A | `#audit` L551–560 | GitHub Action references were mutable. | ALREADY FIXED | Actions use reviewed immutable revisions; automated update proposals remain review-gated. | Static workflow check rejects tag/branch refs; update lane cannot bypass review or broaden permissions. | Immutable pins and reviewed dependency-update lane. | `88777d4`, `1fcfa8f` | DONE |
| REP-08B | `#audit` L551–560 | ARM cross-install input was mutable and lacked immutable artifact verification. | ALREADY FIXED | Cross-tool artifacts are version-pinned and checksum verified. | Download without exact checksum fails before execution. | Workflow/policy verifier covers pinned installer and checksum. | `88777d4` | DONE |
| REP-09 | `#audit` L563–572 | README counts had drifted from 25 to 28 crates and 247 to 543 Rust files. | CONFIRMED CURRENT; prior fix regressed | README project statistics are generated from tracked source and verified in the canonical gate. | Verifier produces no diff at the committed snapshot. | `python3 scripts/verify_readme_stats.py` currently fails: README says 30 crates and 162,326 lines/578 files; the provisional generator reports 32 crates and 227,205 lines/643 files. | Prior verifier `66d37f2`; new stats commit required | REGRESSED |
| REP-10 | `#audit` L505–515; `#roadmap` L724–731 | The local formatting/strict-Clippy/targeted/full-test batch was useful but not a complete canonical release gate. | PARTIALLY FIXED | Preserve the fast batch path and expose one deterministic full gate covering build/check, lint, tests/docs, architecture, supply chain, MSRV, packaging, and experiments. | Generated final-gate section records exact commands/results against one clean tree; release workflows call the same tasks. | Canonical tasks, pinned tooling, staged package smoke, and Cargo publication policy exist; this ledger revision has not recorded a complete final integrated run. | `66d37f2`, `d528487`, `66ab4ec`, `937876a` | PARTIAL |

## Public APIs and typed errors

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| API-01A | `#audit` L518–528; `#ground-truth` L704–719 | Reusable diff libraries leaked `anyhow` and erased typed sources into strings. | ALREADY FIXED | `diffy-core` exposes a stable typed error/result; contextual reporting is confined to executable edges. | Public API preserves nested I/O/parse/ingestion sources and documents error behavior. | External public-API tests, affected library/CLI tests, strict Clippy/rustdoc. | `3b2dae7` | DONE |
| API-01B | `#audit` L518–528; `#ground-truth` L704–719 | Reusable search libraries leaked `anyhow` and erased typed sources. | ALREADY FIXED | Search core exposes stable typed errors; CLI/daemon adapters add presentation context. | Public signatures contain no `anyhow::Result`; source chains survive conversion. | Search public-API and error-source tests. | `0a1039a` | DONE |
| API-01C | `#audit` L518–528; `#ground-truth` L704–719 | Reusable daemon libraries leaked `anyhow` and erased typed sources. | ALREADY FIXED | Daemon core/protocol expose typed errors and reserve `anyhow` for binary/CLI edges. | Public signatures are typed, compatibility remains, and nested causes are inspectable. | Daemon public-API, protocol, and conversion tests. | `3aa563f` | DONE |
| API-01D | `#audit` L518–528; `#ground-truth` L704–719 | Reusable test libraries leaked `anyhow` and erased typed sources. | ALREADY FIXED for `testy-core` | `testy-core` exposes stable typed errors and semantic exit behavior remains at the executable edge. | Public core signatures contain no `anyhow::Result`; tests preserve source/context. | External/core typed-error tests. | `323a68f` | DONE |
| API-01E | `#audit` L518–528; `#ground-truth` L704–719 | Reusable test-library seams beyond core leaked `anyhow`. | ALREADY FIXED | `testy-cli-common` reusable APIs expose typed errors; only top-level CLI reporting may use `anyhow`. | Public signatures in `impact.rs`, `shard.rs`, `testmap.rs`, and `support.rs` no longer expose `anyhow::Result`. | External public-error tests cover every reusable seam, source chains, contextual hints, and CLI message/exit compatibility. | `8e18750` | DONE |

## Documentation

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| DOC-01 | `#audit` L533–545 | Strict rustdoc found broken intra-doc links in `testy-cli-common/impact.rs` and `covy-cli/cmd_map_paths.rs`; the pass also exposed invalid HTML placeholder tags. | ALREADY FIXED | Strict workspace rustdoc has no broken link or invalid HTML warning. | `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --all-features --no-deps --locked` succeeds. | Both links and 12 invalid placeholder tags were repaired; strict rustdoc passed at the closure snapshot. | `3db342d` | DONE |
| DOC-02A | `#audit` L533–572; `#ground-truth` L704–719 | Stable packet interfaces largely lacked crate/item docs, examples, and relevant Errors/Panics/Safety contracts. | ALREADY FIXED for the selected packet surface | Selected stable packet contracts have crate/module docs, examples, and explicit behavioral/error contracts. | Strict rustdoc passes and public contract examples/compile checks cover the supported packet surface. | The packet crate now inventories every public module/root compatibility export, classifies operational, compatibility-only, data-contract, and internal exclusions with reasons, and runs envelope/hash, artifact, and schema-registry examples. | `b3ac32f`, `e5cd23e`, `6062f5d` | DONE |
| DOC-02B | `#audit` L533–572; `#ground-truth` L704–719 | Stable ingestion interfaces largely lacked crate/item docs and examples. | ALREADY FIXED for the audited public ingestion surface | Ingestion public APIs document format behavior, errors, and a runnable path-ingestion example. | Strict package rustdoc and doctest pass for supported ingestion entrypoints. | Public ingestion documentation and runnable example coverage. | `8816e73`, `829ebe7` | DONE |
| DOC-02C | `#audit` L533–572; `#ground-truth` L704–719 | Stable kernel interfaces largely lacked crate/item docs, examples, and contracts. | ALREADY FIXED for the selected kernel surface | Stable scheduler/lifecycle/kernel APIs document state, failures, invariants, and relevant Panics/Safety behavior. | A generated public-item inventory has no unexplained selected gaps; strict rustdoc/doctests pass. | Mechanism, builtins, and compatibility docs record exact 35/53/45 root inventories; composition, scheduler/cancellation, persistence lifecycle/fallback, all 12 error codes, panic boundary, and exclusions are documented and mechanically checked. | `e5cd23e`, `1d7b981` | DONE |
| DOC-02D | `#audit` L533–572; `#ground-truth` L704–719 | Stable daemon interfaces largely lacked crate/item docs, examples, and contracts. | ALREADY FIXED for the selected daemon runtime surface | Protocol/client/core public APIs document framing, lifecycle, migration, errors, and compatibility. | Strict daemon rustdoc and all selected runnable/compile examples pass; a source-checked inventory classifies every selected module and root compatibility group with explicit exclusions. | The exact inventory covers 10 protocol modules plus its two-name root allowlist, two client modules, five core modules plus its frozen 182-name root facade, and both `packet28d` exports. Six runnable examples, two compatibility compile-fail examples, and one feature-gated `no_run` example are mechanically anchored; strict Rustdoc and all nine examples pass. | `b3ac32f`, `e5cd23e`, `3aa563f`, `0128ac14` | DONE |

## Tests and test infrastructure

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| TST-01 | `#audit` L563–572 | `suite-ingest` had no direct tests. | ALREADY FIXED | The crate directly tests format detection, explicit formats, merge/deduplication, and failures. | Direct unit suite and at least one public example execute in the package gate. | Seven direct unit tests plus one doctest at closure. | `8816e73` | DONE |
| TST-02 | `#audit` L563–572 | `testy-cli-common` had no direct tests. | ALREADY FIXED | The crate directly tests parsing, delegation, adapter conversion, ingestion, and contextual errors. | Direct package tests execute without relying only on downstream integration binaries. | Fourteen direct unit tests at closure. | `0538840` | DONE |
| TST-03A | `#audit` L563–572 | No property-style coverage existed for protocol schemas/framing. | ALREADY FIXED | Deterministic generated cases cover bounded frame sizes, tags, malformed/truncated/oversize input, and wire compatibility. | Full boundary range/representative generators preserve stable serialization and reject invalid frames. | Variable-size frame round trips, exhaustive discriminator tests, and four exact request goldens. | `b3ac32f` | DONE |
| TST-03B | `#audit` L518–528, L563–572 | No property-style coverage existed for parser/index corruption boundaries. | ALREADY FIXED | Generated corruption cases cover every record/row boundary, overflow/range error, publication failure, clear, and retry. | Invalid layers never publish or become clean misses; valid EOF/recovery remains deterministic. | Search corruption suite, authenticated-generation cases, fuzz corpus, and durable clear/retry state coverage. | `7ad5adf`, `d1da15d`, `a24202b`, `32c82da` | DONE |
| TST-03C | `#audit` L518–528, L563–572 | No property-style coverage existed for lifecycle/state invariants. | ALREADY FIXED | Legal lifecycle transitions and legacy wires are exhaustively covered; invalid typed states fail to compile or deserialize. | Transition matrix, compatibility, cancellation race, and compile-fail cases all execute. | Typed lifecycle, cancellation, and protocol constraint suites. | `a8f3d4e`, `e5cd23e`, `fe5061f` | DONE |
| TST-03D | `#audit` L563–572 | No generated invariant coverage existed for test-plan equivalence. | ALREADY FIXED | Sparse planner output is exactly equivalent to the frozen dense reference over deterministic generated cases. | All generated test/file/line fixtures produce identical plans. | 192-seed sparse-vs-dense parity suite. | `d2b86f0` | DONE |
| TST-04 | `#audit` L563–572 | No fuzz coverage existed. | ALREADY FIXED | Non-default fuzz targets/corpus exercise untrusted protocol/index/parser boundaries with a bounded smoke runner. | Targets compile; bounded smoke command runs seeded corpus and is exposed to canonical tooling. | Fuzz package, targets, corpus, and smoke tests landed. | `d1da15d` | DONE |
| TST-05A | `#audit` L563–572 | Structural daemon protocol output lacked snapshot coverage. | ALREADY FIXED | Representative requests and all discriminators have reviewed stable structural fixtures. | Complete normalized values, not fragments, are compared to committed goldens. | Four daemon request JSON fixtures plus exhaustive tag tests. | `b3ac32f` | DONE |
| TST-05B | `#audit` L563–572 | Structural MCP output lacked snapshot coverage. | ALREADY FIXED for the native lifecycle seam | Native tool schema/parse/execute/summary/result structure has reviewed lifecycle fixtures. | Tool-list and per-family output drift fails a focused test. | `native_tool_lifecycle.json` and structural lifecycle test. | `ee0b556` | DONE |
| TST-05C | `#audit` L563–572 | Existing packet snapshots were shallow rather than broad structural contracts. | ALREADY FIXED for the selected stable families | Selected stable packet families have reviewed full-value schema snapshots. | A documented inventory identifies every stable packet family as covered or explicitly excluded. | A registry-derived invariant maps all 14 stable packet families to exactly compact/full/handle fixtures, validates full wrapper and payload shape, permits only reasoned explicit exclusions, and adds only the three missing `suite.map.query.v1` fixtures. | `6062f5d` | DONE |
| TST-06 | `#audit` L563–572 | No compile-fail coverage existed for invalid public API/state usage. | ALREADY FIXED for selected lifecycle constraints | Invalid typed lifecycle/public-state usage is proved impossible with compile-fail coverage. | Compile-fail examples execute in the normal docs/test gate and fail for the intended reason. | Protocol/lifecycle `compile_fail` doctests landed. | `e5cd23e` | DONE |
| TST-07A | `#audit` L533–572 | The audit ran zero doctests and stable ingestion APIs had no runnable example. | ALREADY FIXED | A public end-to-end ingestion example runs as a doctest. | Workspace/package doctest command executes and the example validates decoded output. | `suite-ingest` runnable LCOV ingestion doctest. | `8816e73` | DONE |
| TST-07B | `#audit` L533–572 | Stable packet, kernel, and daemon APIs lacked happy-path doctest examples. | ALREADY FIXED for selected packet, kernel, and daemon entrypoints | Selected stable entrypoints have runnable examples and workspace doctest inventory is nonzero and intentional. | Generated inventories list covered/excluded APIs; the full doctest gate passes. | Packet has three reviewed operational-family examples; kernel has five mechanism/composition/compatibility examples plus two compatibility compile-fail cases. Daemon runtime adds six runnable examples, two compatibility compile-fail examples, and one feature-gated `no_run` example. Exact source-marker inventories and focused doctest gates pass. | Partial `b3ac32f`, `e5cd23e`; packet `6062f5d`; kernel `1d7b981`; daemon `0128ac14` | DONE |
| TST-08A | primary candidate: `#test-harness` L419–467; repeated audit summary: `#audit` L563–572 | Rated **Worth exploring · local-substitutable**: 142 integration binaries and 63 support files repeated helpers and nested Cargo builds. | ALREADY FIXED for the reviewed shared harness surface | Reused path/build/process/MCP behavior lives behind one deep harness; truly local fixtures remain local. | Source inventory shows deliberate residual local helpers and no unnecessary nested builds in migrated workflows. | `scripts/check_test_harness.py` passes with one lifecycle owner, one nested-Cargo owner, one Git owner, 17 bounded process clients, four reviewed peer-framing fixtures, and two bounded local-socket fixtures. | `b67386b`, `8db650c`, `c6bc995c` | DONE |
| TST-08B | `#test-harness` L419–467 | One-off process helpers leaked timeout, child, and cleanup ownership. | ALREADY FIXED for migrated workflows | A deep RAII harness owns processes, MCP I/O, timeouts, temporary resources, and failure cleanup. | Harness self-tests prove success/error/timeout/drop cleanup; representative workflows use it. | Harness self-tests pass 12/12; native MCP 3/3 and proxy 15/15 run through bounded owners, including deterministic stdout-backpressure failure and cleanup. | `b67386b`, `8db650c`, `c6bc995c` | DONE |

## Persistence, cache, index, SQLite, and compute performance

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| PER-01A | `#thread-addendum` L600–604 | Every task event was appended and then the complete task registry was serialized and atomically written with `sync_all` while the daemon-wide mutex remained held. | ALREADY FIXED; revalidated at `e763ff3f` | Task call sites stage only changed keyed records under the daemon lock. One owner appends revisioned checksummed WAL batches and events outside it; event append fences every causally prior registry revision, while full checkpoints are debounced. The compact authority has private membership/revision state, is created only by an authenticated checkpoint-plus-WAL load, consumes and retains the daemon lifecycle lease, and holds an exclusive registry-authority admission lease while supported external registry/event/cleanup writers acquire its shared counterpart. It advances only after successful WAL synchronization and is checked against the caller's replayed startup image. Event and WAL publication therefore do not decode the full checkpoint, cannot be admitted by a fabricated caller registry, and cannot race a supported checkpoint/WAL replacement or failed-initial-task unlink after authority load. The daemon rollback path is routed through the same persistence owner, avoiding self-deadlock while ordering cleanup after prior event commands. New WAL task IDs are checked against exact and filesystem-aliasing managed entries before any frame is written, and replay carries authenticated WAL-admission provenance into checkpoint validation. | Checksum/gap/torn-tail/idempotency tests, injected WAL/checkpoint/event failures, exact/alias anti-adoption regressions, fabricated-registry rejection, wrong-root/wrong-role lease rejection, retained-lease lifecycle exclusion, supported-writer and cleanup exclusion until authority drop, persistence-owner cleanup/event ordering, a post-load malformed-checkpoint sentinel proving the event fast path performs no authority reread, WAL-only event/shutdown recovery, admission/restart reconciliation, delayed-launch ordering, the single-I/O-owner source guard, compile-time size bounds, and focused daemon suites cover the lifecycle. | Current release capture: 300-task legacy path 59,150,656 bytes/32 checkpoints/83,121,667 ns median event lock hold versus owned WAL 193,096 bytes/0 measured checkpoints/4,750 ns; total median elapsed 2,657,956 µs versus 439,297 µs. The owned byte ratio remains 1.0 for 1 versus 300 tasks. Core 374/374, daemon persistence 17/17 plus 20/20 with one benchmark ignore, lifecycle integration 6/6, complete packet28d tests before the final cleanup fix, strict two-package all-target/all-feature Clippy, and strict rustdoc pass. | `583c8ad0`, `6cfc76b1`, `e15fc73b`, `ac6200e6`, `5338d06f`, `19492cc5`, `4bfab16a`, `0d8933fa`, `869581bf`, `11b01a56`, `937ac358`, `e93eeca6`, `e566c949`, `e763ff3f` | DONE |
| PER-01B | `#thread-addendum` L600–604 | The complete watch registry was likewise serialized/synced while the daemon-wide mutex remained held. | ALREADY FIXED; revalidated at `e566c949` | Watch mutations stage keyed upsert/removal deltas with stable insertion-order metadata. The persistence owner coalesces them, synchronizes the WAL outside the global lock, and folds them into paired checkpoints on the owned lifecycle. The task-only zero-watch-scan experiment from `0dc52344` was rejected after blind review because it bypassed the public `RegistryDeltaBatch::apply_to` duplicate-watch validation contract; `e93eeca6` restores failure-atomic validation. The exclusive authority/shared writer admission in `e566c949` also prevents a supported watch-registry replacement from invalidating the owner's authenticated membership snapshot. The scan remains outside the daemon-wide lock, while WAL publication stays proportional to changed records. | Concurrent task/watch mutation, an explicit task-only duplicate-watch compatibility regression, supported replacement-writer exclusion, full-scan instrumentation, watch lock-release, autonomous watch-metadata replay, checkpoint/WAL failure retry, relationship validation, shutdown, startup-before-readiness, and compile-time WAL capacity checks mechanically cover parity and failure paths. | The current persistence modules pass 36 focused daemon tests with one release-only benchmark ignored during debug runs; the complete core suite passes 373/373. The explicit release benchmark passes and publishes the same 193,096 bytes for 1-task and 300-task registries. The rejected zero-scan hypothesis is preserved as historical evidence rather than presented as a shipped optimization. | `ea7fe655`, `583c8ad0`, `6cfc76b1`, `e15fc73b`, `ac6200e6`, `5338d06f`, `0d8933fa`, `869581bf`, `0dc52344`, `937ac358`, `e93eeca6`, `e566c949` | DONE |
| PER-02 | `#thread-addendum` L606–610 | Context-cache writes evicted, collected/cloned all live entries/indexes, serialized the full envelope, and wrote while holding the cache mutex. | ALREADY FIXED | Dirty deltas/checkpoints are encoded and written by a debounced persistence owner outside kernel locks, with root/lifecycle/fault fencing. | Concurrency, crash windows, cross-root changes, bounded flush, and fault recovery preserve data; lock/write benchmarks show bounded work. | Versioned benchmark plus persistence-owner lifecycle, crash, root, and fault tests. | `4bc30e6`, `d6d3f4d`, `cac2ae3`, `e99b584`, `a7c2107`, `4d4991e`, `8eed08d` | DONE |
| PER-03A | `#thread-addendum` L612–616 | A one-file map update cloned the complete repository snapshot and rewrote it. | ALREADY FIXED | Owned incremental generations publish immutable updates without cloning/rewriting the full repository. | Update/delete/retry/compaction preserve reader parity and generation integrity; benchmark scales with changed work. | Incremental generation runtime, integrity/writer fencing, ingress recovery, path-collision protection, daemon adoption, and release evidence. | `948aae6`, `bd9fe24`, `a24202b`, `29fd2fb`, `6a49f03`, `b474315`, `4777e0a`, `32c82da` | DONE |
| PER-03B | `#thread-addendum` L612–616 | Regex overlay updates reread every prior overlay document, rebuilt all overlay files, and reloaded the base layer. | ALREADY FIXED | Immutable overlay segments/tombstones retain base generation ownership and compact by threshold. | Update/delete/compaction/recovery/concurrent-reader tests preserve query parity without full overlay rebuild. | Immutable overlay segments, generation ownership, authenticated records, and incremental benchmark artifacts. | `832e40f`, `ee940b0`, `a24202b`, `bd2adb8` | DONE |
| PER-04A | `#thread-addendum` L618–622 | Each local-memory connection reran schema setup and rebuilt five FTS tables. | ALREADY FIXED | `PRAGMA user_version` migrations and one store/connection owner make schema initialization versioned and idempotent. | Migration from every supported version and repeat open do not rebuild current FTS/schema; rollback preserves prior state. | Versioned migration/connection lifecycle tests. | `9c04e11` | DONE |
| PER-04B | `#thread-addendum` L618–622 | Graph workflows composed operations that independently reopened the database. | ALREADY FIXED | Graph/memory/feedback workflows share owned connections, prepared statements, and batch transactions. | Connection-count telemetry and rollback tests prove one workflow transaction; homogeneous parameters stream without temporary collection. | Workflow transaction tests and streaming SQLite parameter refactor. | `9c04e11`, `8c1f770` | DONE |
| PER-05A | `#thread-addendum` L624–628 | Test planning materialized a dense tests × files matrix. | ALREADY FIXED | Test-map v3 persists sorted non-empty bitmap cells with explicit framing/migration. | v1/v2 migration and corruption checks pass; sparse-vs-dense plan output is identical. | 192-seed parity and storage/malformed-boundary suites; release fixture artifact shrank 10.9×. | `d2b86f0`, `153a3e3` | DONE |
| PER-05B | `#thread-addendum` L624–628 | Planning rebuilt Roaring bitmaps and cloned remaining bitmaps during gain calculation. | ALREADY FIXED | Candidate overlaps are precomputed and remaining sets are intersected/borrowed without per-candidate cloning. | Frozen planner parity holds; release planning benchmark demonstrates the intended scaling improvement. | Controlled fixture improved median planning from 654,174 µs to 10,091 µs while preserving exact output. | `d2b86f0`, `153a3e3` | DONE |
| PER-06A | `#thread-addendum` L630–634 | Map and regex indexes independently walked and read the repository. | ALREADY FIXED | Feature-gated scan sessions share bounded path/content work and daemon full-index composition consumes one scan. | Map/search outputs remain equivalent; production harness shows one shared scan and reports time/allocation evidence. | Shared-scan APIs, daemon composition, controlled and production benchmark artifacts. | `e63d930`, `b74c5e0`, `0a68f88`, `a905dc1`, `c65a5dc`, `9e9c022` | DONE |
| PER-06B | `#thread-addendum` L630–634 | Diagnostic ingestion read a whole file and then copied it into a second buffer. | ALREADY FIXED for the measured path | Detection and parsing share the input buffer; streaming/mmap changes are deferred unless measurement justifies them. | Boundary parity holds and allocation/time probes show no target regression. | Diagnostics allocations reduced 2→1 and requested bytes 50% in versioned measurement. | `7201ddc` | DONE |
| PER-06C | `#thread-addendum` L630–634 | LCOV parsing allocated a vector for each comma-separated record. | ALREADY FIXED | `DA`/`BRDA`/`FNDA` fields are parsed from borrowed iterators without temporary vectors. | Malformed/overflow/extra/comma-containing cases retain behavior and measured allocations fall materially. | Allocations 50,012→12, requested bytes −96.97%, and time −33.93% in recorded release evidence. | `7201ddc` | DONE |
| PER-07 | `#thread-addendum` L636–640 | Map cache identity used second-resolution mtime, so same-size rapid edits could collide. | ALREADY FIXED | Cache identity includes signed nanosecond mtime and BLAKE3-backed content fingerprint. | Same-size/same-mtime edit invalidates; warm reuse, old-version invalidation, invalid UTF-8, and pre-epoch timestamps behave deterministically. | Twenty-seven mapy tests and strict package Clippy at closure. | `13cc5c3` | DONE |
| PER-08 | `#thread-addendum` L636–640 | No bounded retention policy was found for the local task/artifact tree. | ALREADY FIXED | Supported inspect, dry-run planning, explicit apply, protected-state accounting, journaled recovery, and age/size policy own deletion. | Default is non-mutating; active/corrupt/cross-boundary artifacts are protected; crash/concurrency/partial progress is typed and recoverable. | Retention/live-metrics subsystem, E2E fixtures, lifecycle lease coordination, startup recovery, and retention documentation. | `2c7b688`, `5bd14ef`, `3971736`, `786e017`, `d772328` | DONE |
| PER-09A | `#audit` L518–528 | Roughly 60 redundant clones existed; the largest verified case cloned the full context-section vector before pruning. | PARTIALLY FIXED | Versioned lint inventory and release measurements select only hot-path clone removals; rejected changes remain documented. | Current inventory, source hash, before/after result, and rejection rationale are reproducible. | Recorded current inventory is 65 redundant clones; largest audited clone and selected routed-argv/generator clones were removed. | `4635b6c`, `932c4b4`, `3237e6b`, `b736639` | PARTIAL |
| PER-09B | `#audit` L518–528 | Line counting and coverage reporting collected intermediate vectors. | PARTIALLY FIXED | Verified hot intermediate collections are removed or retained with benchmark justification. | Focused `needless_collect` inventory and affected behavior tests are recorded; no blanket zero-collection claim. | Three selected production collections were removed; current lint inventory still reports five candidates. | `4635b6c` | PARTIAL |
| PER-09C | `#audit` L518–528 | Daemon merge helpers allocated nested sets/vectors. | ALREADY FIXED for the measured broker focus path | Broker focus sets are merged once without repeated nested materialization. | Plan/focus output remains deterministic and allocation/behavior evidence covers the changed path. | Broker render/search-plan merge refactor plus plan validation regression. | `969cc75` | DONE |
| PER-09D | `#audit` L518–528 | Query/budget structs of 48–88 bytes were said to be repeatedly passed by copy. | NOT REPRODUCIBLE | Do not make a code change unless a controlled current-source inventory reproduces the claim and measurement shows value. | Versioned focused lint inventory is zero or identifies exact types/calls and a benchmark. | Current recorded `large_types_passed_by_value` inventory is zero. | `4635b6c`, checksum correction `932c4b4` | NOT REPRODUCIBLE |
| PER-10 | `#thread-addendum` L588, L646 | Historical workspace-index build was 10,375.754 ms on 31 March 2026 for p28 `59e54fb` / packet28d `0.2.39`; it was not current-HEAD evidence. | HISTORICAL; current evidence exists for an earlier remediation snapshot | Historical evidence remains immutable; current runs use an isolated, locked, versioned harness with raw source/host/toolchain metadata and no cross-environment ratio. | Final integrated HEAD is rerun three times and artifact identity excludes generated reports/live `.packet28`. | Harness tests and snapshot at `ee0b556` recorded 18,826.714 / 21,507.034 / 22,670.113 ms; a final provisional-`1d7b981` rerun is not recorded. | `aee9028` | EVIDENCE ONLY |
| PER-11A | `#thread-addendum` L585, L646 | Live task registry was 1,848,193 B and exactly 300 records at 2026-07-28 11:55 EDT. | HISTORICAL / VOLATILE | Supported inspect metrics report bytes/count/reliability/time; the sampled number is never a constant. | Any copied snapshot retains timestamp, scope, schema version, and reliability. | Versioned retention metrics and archived audit snapshot exist. | `2c7b688` | EVIDENCE ONLY |
| PER-11B | `#thread-addendum` L586, L646 | Whole `.packet28` was 160.97 MiB; the report explicitly said this was volatile and not “artifact size.” | HISTORICAL / VOLATILE | Metrics distinguish whole-store logical/allocated bytes from artifact subsets. | UI/docs never relabel this value as artifact size or a current product constant. | Storage metrics fixtures and retention documentation preserve scope/reliability flags. | `2c7b688` | EVIDENCE ONLY |
| PER-11C | `#thread-addendum` L587, L646 | Task store had 4,610 files; 4,302 were artifact-like and occupied 42.81 MiB on disk. | HISTORICAL / VOLATILE | Inspect/retention reports separate total, artifact-like, protected, quarantine, before/after, logical, and allocated accounting. | Timestamped schema-versioned output can reproduce the distinction without hard-coded values. | JSON/text fixtures, archived snapshot, and retention accounting tests. | `2c7b688` | EVIDENCE ONLY |

## Tokio orchestration boundary

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| ASY-00 | `#thread-addendum` L652–659 | At audit baseline there was no production Tokio dependency; only fixture text matched. | HISTORICAL | Keep this as a baseline fact rather than a current architectural claim. | Later Tokio adoption names its narrow orchestration seams and does not rewrite the baseline. | Production Tokio now exists in MCP/daemon orchestration through later commits. | `a501661`, `8ea0492` | EVIDENCE ONLY |
| ASY-01A | `#thread-addendum` L652–659 | MCP proxy used one serial stdio loop without request-ID-correlated concurrency; a late response could poison later work. | ALREADY FIXED | Tokio orchestration correlates IDs, bounds in-flight/output work, and routes out-of-order/late responses safely. | Fast-before-slow, out-of-order, timeout-then-late, backpressure, framing, and cancellation cases remain correct. | Bounded concurrent MCP orchestration and routing tests. | `a501661` | DONE |
| ASY-01B | `#thread-addendum` L652–659 | The recommended MCP seam included per-upstream timeouts. | NOT REPRODUCIBLE as wholly missing | Preserve existing configurable timeout behavior while adding async routing. | Timeout configuration remains compatible and late timed-out responses cannot poison later requests. | Revalidation found per-upstream timeout configuration already existed; new routing tests cover late responses. | `a501661` | PRESERVE |
| ASY-01C | `#thread-addendum` L652–659 | MCP proxy needed owned child cleanup instead of serial-loop process handling. | ALREADY FIXED | Child/process ownership is bounded and reaped on exit, cancellation, timeout, and drop. | Child-exit/reap and cancellation tests leave no orphaned process. | Owned-child MCP orchestration tests. | `a501661` | DONE |
| ASY-02A | `#thread-addendum` L652–659 | packet28d transport used thread-per-connection. | ALREADY FIXED | Structured Tokio connection tasks are capped, tracked, and joined; blocking/CPU work stays off core workers. | Load/cap/stalled-client/shutdown tests show bounded task/resource counts. | Async daemon transport and architecture boundary tests. | `8ea0492`, `0e3aa17` | DONE |
| ASY-02B | `#thread-addendum` L652–659 | TCP/Unix shutdown and subscriber lifecycle were split rather than one owned lifecycle. | ALREADY FIXED | One lifecycle owns bounded channels, deadlines, cancellation, connection tasks, and TCP/Unix stop. | TCP/Unix parity, lag, stalled I/O, stop, and endpoint-release tests pass. | Structured transport lifecycle tests. | `8ea0492` | DONE |
| ASY-03A | `#thread-addendum` L652–659 | Wait/watch/notification paths used sleep/poll loops. | ALREADY FIXED for the audited daemon and MCP notification owners | Owned timers/signals replace polling at async seams; unavoidable external polling is explicit and bounded. | Source inventory finds no unowned sleep loop in migrated daemon/watch paths and identifies any deliberate CLI polling. | Daemon watch uses owned timers; MCP and MCP-proxy notification work is signal-driven behind one bounded owner with pause/resume, offset, shutdown, and output-backpressure tests. | `8ea0492`, `e142e57` | DONE |
| ASY-03B | `#thread-addendum` L652–659 | One synchronous debounce processor owned notification work. | ALREADY FIXED | Debounce uses bounded task ownership/timers with deterministic overflow, cancellation, and shutdown. | Paused-time debounce tests cover ordering, overflow, cancellation, and stop without runtime starvation. | The supervised daemon runtime owns debounce and notification lifecycle; paused-time, saturation, cancellation, and bounded shutdown tests pass. | `8ea0492`, `59bd703` | DONE |
| ASY-04A | `#thread-addendum` L662–668 | Tokio does not make parsing, scans, bitmap work, index construction, SQLite, serialization, or filesystem writes non-blocking. | CONFIRMED CURRENT design constraint | Runtime architecture continues to distinguish async orchestration from CPU/blocking ownership. | Architecture test rejects known blocking work on core runtime tasks; benchmark detects worker starvation. | The source-authenticated release artifact records direct synchronous p95 timer lateness of 14,103 µs versus 2,083 µs through the bounded blocking pool; the ordinary held-work timer regression also passes. | `0e3aa17`, `8ea0492`, `186fbebe` | DONE |
| ASY-04B | `#thread-addendum` L662–668 | Rayon should remain for data parallelism; bounded workers or `spawn_blocking` should isolate blocking work. | CONFIRMED CURRENT design constraint | CPU work remains on Rayon/bounded blocking pools rather than core Tokio workers. | Source/architecture checks and starvation fixture show bounded async latency under CPU work. | The release artifact mechanically binds the runtime source and records an 85.230% p95 lateness reduction (6.771×) through the bounded blocking owner. | `0e3aa17`, `186fbebe` | DONE |
| ASY-04C | `#thread-addendum` L662–668 | Synchronous SQLite or full-snapshot writes must not be placed in async tasks and called non-blocking. | ALREADY FIXED at `869581bf` | SQLite remains behind bounded blocking ownership; registry WAL/event/checkpoint I/O is confined to the dedicated persistence owner and is never mislabeled as async. Tokio transport orchestration crosses the established blocking boundary. | Async-boundary architecture checks plus the packet28d single-persistence-owner source guard reject direct registry/event writers; lock-release, stalled-I/O, shutdown, and full parallel/serial daemon suites cover the runtime boundary. | Strict all-target/all-feature packet28d Clippy and focused architecture/persistence tests pass; PER-01A/B now close the previously open persistence prerequisite. | `0e3aa17`, `6cfc76b1`, `e15fc73b`, `ac6200e6`, `0d8933fa`, `869581bf` | DONE |

## Stable instruction prefix and controlled experiment

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| EXP-01 | `#stable-instruction-prefix` L367–417 | Rated **Strong direction · experiment-gated**: real shim requests omitted task ID; active task/snapshot focus affected selection, excerpts, focus line, and header, so identical source could render different bytes. | ALREADY FIXED | Stable rendering depends only on source bytes, canonical path, effective schema/budget, renderer version, and normalized stable repo config; mutable state stays in the brief. | Stable bytes/hash/cache remain invariant across task/backend/agent/snapshot; every stable input miss boundary is covered; adaptive output is uncached or fingerprinted. | Kernel invariance/miss-boundary tests and daemon brief-vs-prefix regression. | `0456c30`, `a031406` | DONE |
| EXP-02A | primary candidate: `#stable-instruction-prefix` L409–415; repeated addendum: `#thread-addendum` L672–680 | Passthrough—untouched instruction content—must remain the baseline/default. | ALREADY FIXED | Missing/invalid/unreadable/unsupported configuration fails open to byte-exact passthrough. | Default request is byte exact and never populates stable/adaptive cache; old requests/responses decode compatibly. | Default/no-cache, precedence, malformed/path/symlink fail-open, and legacy-adapter compatibility tests. | `0456c30`, `a031406` | DONE |
| EXP-02B | primary candidate: `#stable-instruction-prefix` L409–415; repeated addendum: `#thread-addendum` L672–680 | Stable mode is deterministic compression from source plus stable configuration only. | ALREADY FIXED | Stable mode is explicit, opt-in, cacheable only by stable identity, and emits compatibility-safe telemetry. | Stable hash is invariant across mutable task/snapshot transitions and changes for every stable input. | Stable rewrite/cache/invariance tests and controlled harness records. | `0456c30`, `a031406`, `33f2ee0` | DONE |
| EXP-02C | primary candidate: `#stable-instruction-prefix` L409–415; repeated addendum: `#thread-addendum` L672–680 | Adaptive mode is the current task/snapshot-sensitive experimental comparator. | ALREADY FIXED | Adaptive mode is explicit and uncached unless keyed by a canonical render-relevant snapshot fingerprint. | Same-task render-relevant drift changes output; irrelevant drift does not; no stale adaptive hit occurs. | Adaptive fingerprint/miss tests and controlled harness records. | `0456c30`, `a031406`, `33f2ee0` | DONE |
| EXP-03A | `#thread-addendum` L681–684 | Required scenario: cold start. | ALREADY FIXED in harness | Versioned manifest executes cold start for passthrough, stable, and adaptive modes. | Three repeated raw records per mode retain source/config/environment identity. | Manifest-driven real-kernel harness and versioned JSONL artifacts. | `33f2ee0` | DONE |
| EXP-03B | `#thread-addendum` L681–684 | Required scenario: second request. | ALREADY FIXED in harness | Second request measures local eligibility/hit without changing stable inputs. | Stable may hit; passthrough remains byte exact; adaptive remains deliberately uncached. | Controlled harness invariants and raw records. | `33f2ee0` | DONE |
| EXP-03C | `#thread-addendum` L681–684 | Required scenario: compaction. | ALREADY FIXED in harness | Compaction transition records prefix hash and rewarm-related fields without inventing provider data. | Stable hash remains identical when stable inputs are unchanged; unknown provider metrics remain unknown. | Controlled harness transition record. | `33f2ee0` | DONE |
| EXP-03D | `#thread-addendum` L681–684 | Required scenario: active task A→B. | ALREADY FIXED in harness | Stable bytes remain task-invariant while adaptive/brief state may change. | Stable hash is identical across A→B; passthrough is exact; adaptive behavior is recorded. | Controlled harness transition record. | `33f2ee0` | DONE |
| EXP-03E | `#thread-addendum` L681–684 | Required scenario: snapshot drift. | ALREADY FIXED in harness | Stable bytes ignore mutable drift; adaptive identity responds only to render-relevant drift. | Hash/cache invariants distinguish stable, relevant adaptive, and irrelevant adaptive changes. | Controlled harness and kernel fingerprint tests. | `a031406`, `33f2ee0` | DONE |
| EXP-03F | `#thread-addendum` L681–684 | Required scenario: fresh-worker handoff. | ALREADY FIXED in harness | A fresh worker receives deterministic stable bytes from the same stable inputs. | Stable hash matches prior worker; local/provider cache state is reported separately. | Controlled harness transition record. | `33f2ee0` | DONE |
| EXP-04A | `#thread-addendum` L685–696 | Required metric: `churn_rate = creation / (creation + read)`. | PARTIALLY FIXED; provider values unknown | Formula consumes observed provider creation/read tokens only and yields unknown for missing/invalid evidence. | Complete, partial, zero, and empty cases are versioned schema-tested; no local byte estimate is substituted. | Telemetry schema/formula tests pass; generated experiment report lists provider values as unknown. | `33f2ee0` | EVIDENCE ONLY |
| EXP-04B | `#thread-addendum` L685–696 | Required metric: `reuse_multiple = read / max(creation, 1)`. | PARTIALLY FIXED; provider values unknown | Formula uses observed provider tokens and preserves explicit unknowns. | Complete/partial/zero/empty cases behave exactly as specified. | Telemetry schema/formula tests; provider values remain unknown. | `33f2ee0` | EVIDENCE ONLY |
| EXP-04C | `#thread-addendum` L685–696 | Required metric: `effective_cost = creation_cost + read_cost`. | PARTIALLY FIXED; provider values unknown | Creation/read cost remain separate observed fields and aggregate only when evidence is complete. | No token/byte proxy is promoted to monetary/provider cost. | Schema/formula tests and report unknown markers. | `33f2ee0` | EVIDENCE ONLY |
| EXP-04D | `#thread-addendum` L685–696 | Required metric: `compaction_rewarm_tokens`. | PARTIALLY FIXED; provider values unknown | Rewarm tokens are a distinct observed provider measurement, not inferred from local bytes. | Compaction records retain observed value or explicit unknown. | Versioned telemetry schema and transition record. | `33f2ee0` | EVIDENCE ONLY |
| EXP-04E | `#thread-addendum` L690–696 | Success requires stable hashes plus lower effective cache cost without harming instruction adherence; aggregate hit rate alone is insufficient. | REVALIDATION REQUIRED with provider data | Evaluation combines prefix stability, provider cost, and adherence method/result; local hit rate is diagnostic only. | A provider-instrumented run has complete cost/order/boundary/adherence evidence and meets all criteria. | Local harness proves hash/local-cache invariants but explicitly lacks provider placement, cost, and adherence measurements. | Instrumentation `33f2ee0`; provider run pending | PENDING |
| EXP-05 | primary candidate: `#stable-instruction-prefix` L409–415; repeated addendum: `#thread-addendum` L695–696 | “20K tokens lost,” “100% misses,” and guaranteed net savings were unsupported until provider ordering/cache boundaries were measured. | CONFIRMED CURRENT evidence boundary | User-facing claims remain qualified as historical, hypothesis, estimate, unverified, or provider-measured. | Claim verifier rejects unqualified variants and report states provider/net-savings evidence is not established. | Six claim-lint tests and live scan passed at closure; experiment summary retains unknowns. | `66d37f2`, `33f2ee0` | EVIDENCE ONLY |

## Verified strengths and preservation constraints

These are positive baseline observations, not change requests. “Preserve” does
not mean current evidence may be skipped.

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| INV-01 | primary verdict: Header L52–55; repeated positive evidence: `#audit` L505–515 | Workspace dependency graph was acyclic and most edges pointed toward packet/foundation contracts. | PRESERVED; current graph recheck available | Graph stays acyclic and remediations improve rather than invert contract direction. | Locked metadata graph is acyclic; negative-dependency tests pass for named seams. | Workspace policy and daemon/async architecture guards; protocol extraction reduced implementation edges. | `018b74a`, `73c535a`, `0e3aa17` | PRESERVE |
| INV-02 | `#audit` L505–515 | One typed scheduler interface hid DAG validation, mutation, ordering, and budget enforcement. | PRESERVED after ARC-02 | Scheduler depth and typed behavior survive moving built-ins to composition. | Existing scheduler behavior/ordering/budget suites remain green before and after ARC-02. | Mechanism/builtins scheduler, dependency ordering, budget, cancellation, compatibility, and routing suites pass after the split. | `4468ed8`, `95f404b`, `0888948`, `c9060ca` | PRESERVE |
| INV-03 | `#audit` L505–515 | Behavioral coverage was broad: 1,099 test functions across 26 of 28 crates; the full locked suite passed before the concurrent local edit. | HISTORICAL baseline; current inventory/gate pending | Broad deterministic coverage is retained while missing direct-test gaps close. | Generated current test inventory and clean full locked suite are attached to one tree; baseline count remains unchanged. | TST-01/02/03/04/05/06/08 add focused coverage, but generated final inventory/gate is pending. | Multiple; see TST rows | PRESERVE |
| INV-04A | `#audit` L505–515 | No production `&String` or `&Vec<T>` parameters were found; strings, slices, and paths were borrowed idiomatically. | REVALIDATION REQUIRED | Public/internal APIs continue to borrow view types where ownership is unnecessary. | Focused source/Clippy inventory remains clean or every exception is justified by ownership/lifetime needs. | Strict Clippy policy exists; no current dedicated inventory artifact is recorded here. | `580954c` policy | PRESERVE |
| INV-04B | `#audit` L505–515 | The main owned trait object and reducer callbacks used dynamic dispatch at real runtime-selection seams. | PRESERVED after composition changes | Dynamic dispatch remains deliberate at runtime substitution boundaries and is not churned without measured/design value. | Source inventory maps each owned trait object/callback to an explicit runtime seam. | The reviewed mechanism inventory names policy, planner, reducer, observer, and hook substitution seams; post-split behavior and compatibility tests pass. | `95f404b`, `0888948`, inventory `1d7b981` | PRESERVE |
| INV-05 | `#audit` L505–518 | Unsafe code stayed peripheral—FFI, mmap, signals, and syscalls rather than core algorithms. | PRESERVED by policy; current inventory should be generated | Core algorithms remain safe; peripheral unsafe contracts are documented and mechanically inventoried. | Generated unsafe inventory is bounded and architecture/policy checks reject undocumented expansion. | FFI fix and unsafe/panic policy; search clear/retry work remains safe typed state. | `0f708f1`, `571a53f`, preservation `32c82da` | PRESERVE |
| INV-06A | `#audit` L505–515 | Packet hashing had focused deterministic tests. | PRESERVED | Supported hash inputs produce stable outputs across refactors/compatibility facades. | Golden/compatibility tests pass at final tree. | Protocol/packet compatibility suites. | `e56d3f0`, `b3ac32f`, `5389ce5` | PRESERVE |
| INV-06B | `#audit` L505–515 | Schema registry behavior had focused deterministic tests. | PRESERVED | Registry tags/order/compatibility remain stable. | Exhaustive discriminator/schema tests and compile compatibility pass. | Protocol exhaustive tag and typed-lifecycle suites. | `b3ac32f`, `e5cd23e` | PRESERVE |
| INV-06C | `#audit` L505–515 | Route catalog behavior had focused deterministic tests. | PRESERVED | Route identity/order/delegation remain stable while ownership/clones are improved. | Route registry/regression tests pass and no unintended route drift is observed. | Routed-argv clone removal retained route tests. | Preservation `b736639` | PRESERVE |
| INV-06D | `#audit` L505–515 | Scheduler failures had focused deterministic tests. | PRESERVED | Scheduler failure selection and reporting remain deterministic and typed. | Failure-order/golden tests pass, including impossible selection handling. | Explicit impossible-selection error and scheduler suites. | Preservation `4468ed8` | PRESERVE |
| INV-06E | `#audit` L505–515 | Index parity had focused deterministic tests. | PRESERVED | Incremental generations, overlays, corruption handling, clear, and retry remain query-equivalent to frozen behavior. | Parity/property/corruption/recovery tests pass and failed publication never leaks partial state. | Incremental/overlay evidence plus durable clear/retry patch A2. | `948aae6`, `832e40f`, `7ad5adf`, `32c82da` | PRESERVE |

## Sequencing and dependency order

Sequence rows are audit recommendations. They remain traceable even when their
constituent implementation rows are complete.

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| SEQ-00 | `#audit` L494–500 | Fix high-severity correctness findings before more structural refactoring. | HISTORICAL; substantially followed | Correctness prerequisites are fixed or explicitly risk-accepted before dependent structural slices. | Commit map shows COR-01A/B through COR-05A/B closures before or alongside dependent architecture changes; remaining exceptions are declared. | High-severity fixes have named commits; no baseline priority is erased. | See COR rows | EVIDENCE ONLY |
| SEQ-01 | `#top-recommendation` L470–474 | Separate daemon protocol from implementation before deeper MCP and broker work. | HISTORICAL; protocol and MCP portions followed, broker remains open | Protocol contract/consumer guard precedes deeper MCP/broker ownership. | ARC-01 is closed before ARC-03/04 is declared complete. | Protocol extraction/adapters/guard landed before MCP lifecycle; ARC-04 remains pending. | `e56d3f0`, `b3ac32f`, `c7e6470`, `73c535a`, then `ee0b556` | EVIDENCE ONLY |
| SEQ-02A | `#thread-addendum` L594 | Fix ownership and write amplification before allocation micro-tuning. | HISTORICAL; order diverged, now closed | Ownership/write-amplification work is not displaced by cosmetic micro-optimization, and the later closure remains explicit. | PER-01…04 status is visible beside PER-06/09 work; historical ordering is not rewritten. | PER-02…04 and measured PER-06/09 landed first; PER-01 later closed with owned keyed WAL evidence. | See PER rows | EVIDENCE ONLY |
| SEQ-02B | `#thread-addendum` L698–700 | Performance-first order: persistence owner/lock split → incremental generations/segments → SQLite migrations/batch ownership → parser/test-map allocation → Tokio for daemon/MCP orchestration. | HISTORICAL; implementation order diverged | The divergence is documented; completion claims still close the prerequisite ownership risk. | The commit index reconstructs actual order and PER-01 retains independent benchmark and lifecycle evidence. | Incremental index/SQLite/parser/Tokio work landed before the daemon registry persistence owner; all named performance slices are now closed. | See PER/ASY rows | EVIDENCE ONLY |
| SEQ-02C | `#thread-addendum` L662–668 | First remove global lock-held I/O/write amplification; an async runtime cannot repair those ownership defects. | HISTORICAL; prerequisite landed after Tokio seams | Async adoption does not relabel synchronous global-lock I/O as non-blocking. The later owner closes that defect outside the daemon lock. | ASY rows reference PER-01 and architecture evidence rejects lock-held registry/event I/O. | Tokio seams landed first; PER-01A/B subsequently closed the persistence prerequisite with measured and failure-path evidence. | `8ea0492`, `0e3aa17`, `6cfc76b1`, `e15fc73b`, `0d8933fa`, `869581bf` | EVIDENCE ONLY |
| SEQ-03A | `#roadmap` L724–731 | Stage 1—Correctness: ABI, fail-closed config, cancellation, TCP stop, and RAII restoration. | HISTORICAL; committed | Stage constituents retain separate COR rows and evidence. | COR-01A/B through COR-05A/B each have focused evidence and commit trace. | All named stage-1 rows are marked DONE. | See correctness rows | EVIDENCE ONLY |
| SEQ-03B | `#roadmap` L724–731 | Stage 2—Reproducibility: lockfile, MSRV, strict canonical CI, rustdoc, and deny. | HISTORICAL; implementation landed, final gate incomplete | Repro tooling precedes release claims and records one-tree gate evidence. | REP-01…04 close and generated final gate is populated. | Tooling landed; final integrated gate remains placeholder and REP-09 is regressed. | See REP rows | EVIDENCE ONLY |
| SEQ-03C | `#roadmap` L724–731 | Stage 3—Architecture: daemon protocol/runtime, kernel composition, MCP families, broker module. | HISTORICAL; partial | Constituents close in the recommended dependency order. | ARC-01/03 are closed; ARC-02/04 remain explicit rather than silently merged. | ARC-01, ARC-02, and ARC-03 are closed at the provisional snapshot; ARC-04 remains explicit and pending. | See ARC rows | EVIDENCE ONLY |
| SEQ-03D | `#roadmap` L724–731 | Stage 4—Interfaces: typed library errors, narrow re-exports, negative dependency tests, quarantined compatibility. | HISTORICAL; committed for the audited seams | Each reusable library seam and compatibility boundary closes independently. | API-01A…E, ARC-07/08 have separate status/evidence. | All five typed-error seams and the audited compatibility/dependency boundaries have named closing commits and tests. | See API/ARC rows | EVIDENCE ONLY |
| SEQ-03E | `#roadmap` L724–731 | Stage 5—Tests/docs: RAII harness, direct missing-crate tests, property/fuzz/schema snapshots, crate docs, doctests. | HISTORICAL; implementation landed, final integrated gate incomplete | Breadth gaps are not hidden behind one new test or one documented crate. | TST/DOC subrows and current inventories show the exact reviewed families and exclusions. | Packet snapshot/docs, kernel and daemon docs, ingestion, properties, fuzz, compile-fail, and harness inventories are closed; the final one-tree gate remains separately tracked. | See DOC/TST rows | EVIDENCE ONLY |
| SEQ-03F | `#roadmap` L724–731 | Stage 6—Measure: selected performance/clone lints, release benchmarks, then verified allocation and lock-held-I/O changes. | HISTORICAL; implementation evidence landed | Measurement precedes optimization and rejected hypotheses remain recorded. | PER-09 split statuses and versioned benchmark artifacts distinguish fixed, partial, and not reproducible claims. | PER-01 now has exact-source lock/write-amplification evidence; the final integrated one-tree gate remains separately tracked. | See PER rows | EVIDENCE ONLY |

## External ground-truth references

These rows retain the validation basis used by the audit. They do not claim the
linked projects are remediation dependencies or that a later commit was copied
from them.

| ID | Source | Immutable baseline observation | Current-source validation | Implementation / evidence | Test / mechanical invariant | Focused evidence | Closing commit | Status |
|---|---|---|---|---|---|---|---|---|
| EXT-01 | `#ground-truth` L704–719 | Cargo recommends committing `Cargo.lock` when in doubt because it captures the successful dependency graph. | REFERENCE | Reference for REP-01; this row authorizes no independent implementation. | Link and rationale remain attached to the lockfile decision. | https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html | Reference only | REFERENCE |
| EXT-02 | `#ground-truth` L704–719 | Workspace lints require explicit member inheritance. | REFERENCE | Reference for REP-05A; this row authorizes no independent implementation. | Policy/verifier matches Cargo’s inheritance model. | https://doc.rust-lang.org/cargo/reference/workspaces.html#the-lints-table | Reference only | REFERENCE |
| EXT-03 | `#ground-truth` L704–719 | `rust-version` is a support promise that should be tested automatically. | REFERENCE | Reference for REP-02 and REP-03D; this row authorizes no independent implementation. | Exact declared toolchain is exercised in CI. | https://doc.rust-lang.org/cargo/reference/rust-version.html#support-expectations | Reference only | REFERENCE |
| EXT-04 | `#ground-truth` L704–719 | Public Rust interfaces need examples and relevant Errors, Panics, and Safety contracts. | REFERENCE | Reference for DOC-01, DOC-02A–D, TST-07A/B, and COR-01B; this row authorizes no independent implementation. | Documentation acceptance criteria retain these dimensions. | https://rust-lang.github.io/api-guidelines/documentation.html | Reference only | REFERENCE |
| EXT-05 | `#ground-truth` L704–719 | Architecture rules should name what lower modules must not know, making diagrams enforceable. | REFERENCE | Reference for ARC-08 and INV-01; this row authorizes no independent implementation. | Negative dependency rules run mechanically. | https://github.com/rust-lang/rust-analyzer/blob/30e799799fca143f05d28052c7292d9f2ee57386/docs/book/src/contributing/architecture.md#L118-L151 | Reference only | REFERENCE |
| EXT-06 | `#ground-truth` L704–719 | nextest separates machine contracts, runner implementation, filtering, and executable. | REFERENCE | Reference for ARC-01A–C; this row authorizes no independent implementation. | Protocol/runtime separation and dependency direction are mechanically checked. | https://github.com/nextest-rs/nextest/blob/2d39374e3d588626c50ad578d34d30a29bbeaa57/Cargo.toml#L1-L13 | Reference only | REFERENCE |
| EXT-07 | `#ground-truth` L704–719 | nextest’s executable is a thin main delegating to a testable application library; the audit said this matched suite-cli, not packet28d. | REFERENCE | Reference for ARC-04B; this row authorizes no independent implementation. | Thin-main evidence is not misapplied to the wrong binary. | https://github.com/nextest-rs/nextest/blob/2d39374e3d588626c50ad578d34d30a29bbeaa57/cargo-nextest/src/main.rs#L4-L11; `64ea03f` is p28-only follow-up evidence | `64ea03f` does not close ARC-04B | REFERENCE |
| EXT-08 | `#ground-truth` L704–719 | ripgrep keeps reusable crates separate while the executable adds anyhow-style reporting and semantic exits. | REFERENCE | Reference for API-01A–E; this row authorizes no independent implementation. | Typed library boundaries and executable error/reporting edges remain separate. | https://github.com/BurntSushi/ripgrep/blob/f9c05a949d1a0dc8e16dee28ca9605d38611faeb/crates/core/main.rs#L43-L78 | Reference only | REFERENCE |
| EXT-09 | `#ground-truth` L704–719 | MCP SDK optional transports/child adapters are feature-gated, but features do not replace crate seams. | REFERENCE | Reference for ARC-03 and ASY-01A/C; this row authorizes no independent implementation. | Feature choices do not erase lifecycle/module ownership criteria. | https://github.com/modelcontextprotocol/rust-sdk/blob/0dea39d10146b96fbba4c0fcb2db74af822fad4c/crates/rmcp/Cargo.toml#L47-L109 | Reference only | REFERENCE |
| EXT-10 | `#ground-truth` L704–719 | Cancellation, owned task tracking, cleanup, and bounded shutdown should be explicit even for otherwise synchronous systems. | REFERENCE | Reference for COR-03/04/07 and ASY-01C/02B; this row authorizes no independent implementation. | Lifecycle ownership is explicit and mechanically tested. | https://tokio.rs/tokio/topics/shutdown | Reference only | REFERENCE |
<!-- END: SOURCE-DERIVED-AUDIT-ROWS -->

## Source-level deduplication relationships

Every source-derived row remains in the 176-row register. These relationships
make repeated HTML claims explicit without letting a duplicate inherit another
row's validation or status. `alias_of` marks the same or near-identical source
claim in a different role; `facet_of` marks one independently retained facet.
Score, synthesis, sequencing, and external-reference rows that merely support
other rows remain cross-referenced in their own evidence cells rather than
being mislabeled as duplicates.

<!-- BEGIN: SOURCE-AUDIT-RELATIONSHIPS -->
| Source-derived row | Relationship | Canonical row(s) | Retained role |
|---|---|---|---|
| `PROV-03C` | `alias_of` | `DOC-01` | Preserves the audit-time rustdoc failure; DOC-01 owns later closure. |
| `PROV-07` | `alias_of` | `PER-10` | Preserves original benchmark provenance; PER-10 owns later measurements. |
| `COR-01B` | `alias_of` | `REP-05C` | COR owns FFI correctness while REP owns the workspace unsafe-policy facet. |
| `REP-05D` | `alias_of` | `PER-09A` | REP owns lint policy while PER owns measurement-led clone decisions. |
| `SUM-02` | `alias_of` | `SEQ-00` | SUM preserves the verdict; SEQ preserves the remediation order. |
| `COR-07B` | `alias_of` | `ASY-02A` | COR owns the defect closure; ASY owns the Tokio transport boundary. |
| `PROV-03E` | `facet_of` | `REP-05B`, `REP-05C`, `REP-05D` | Keeps the combined audit-time lint counts immutable. |
| `PROV-03B` | `facet_of` | `TST-07A`, `INV-03` | Keeps zero-doctest and full-suite-pass facets separate. |
| `ASY-02B` | `facet_of` | `COR-04`, `COR-07A`, `COR-07B` | Retains the unified transport lifecycle synthesis. |
| `SEQ-02C` | `facet_of` | `SEQ-02A` | Retains the async-specific statement of the ownership-first prerequisite. |
<!-- END: SOURCE-AUDIT-RELATIONSHIPS -->

Repeated source occurrences consolidated inside one row retain role-specific
coordinates: EXP-02A/B/C and EXP-05 preserve candidate plus addendum evidence;
TST-08A preserves candidate plus audit summary; INV-01 preserves verdict plus
positive-invariant evidence.

## Legacy aggregate-ID aliases

The earlier 63-row ledger combined several independent HTML observations under
one aggregate ID. Those aggregates are compatibility aliases only; the
source-derived rows below are canonical and remain individually present. Rows
whose old and canonical IDs are identical need no alias.

<!-- BEGIN: LEGACY-AUDIT-ID-ALIASES -->
| Legacy aggregate ID | Canonical source-derived rows | Reason for split |
|---|---|---|
| `COR-01` | `COR-01A`, `COR-01B` | Variadic ABI correctness and unsafe-contract documentation have distinct evidence. |
| `COR-05` | `COR-05A`, `COR-05B` | Swap RAII and non-destructive recovery ordering are separate failure boundaries. |
| `COR-06` | `COR-06A`, `COR-06B` | Record truncation and lookup-range corruption have separate invariants. |
| `COR-07` | `COR-07A`, `COR-07B` | Queue backpressure and connection lifecycle/deadlines are independently testable. |
| `COR-08` | `COR-08A`, `COR-08B` | Task and search state modeling are separate runtime contracts. |
| `ARC-01` | `ARC-01A`, `ARC-01B`, `ARC-01C` | Protocol extraction, consumer migration, and compatibility quarantine close separately. |
| `ARC-02` | `ARC-02A`, `ARC-02B` | Mechanism/composition ownership and CLI dependency direction are separate checks. |
| `ARC-04` | `ARC-04A`, `ARC-04B` | Broker module locality and thin process bootstrap are independent closures. |
| `ARC-05` | `ARC-05A`, `ARC-05B` | Public memory ownership and SQLite lifecycle ownership have distinct evidence. |
| `REP-03` | `REP-03A`, `REP-03B`, `REP-03C`, `REP-03D` | Workspace build, Clippy, rustdoc, and exact-MSRV CI gates remain separately traceable. |
| `REP-05` | `REP-05A`, `REP-05B`, `REP-05C`, `REP-05D` | Lint inheritance, panic, unsafe, and measured clone policy are distinct observations. |
| `REP-08` | `REP-08A`, `REP-08B` | Action pinning and cross-tool checksum verification have different evidence. |
| `API-01` | `API-01A`, `API-01B`, `API-01C`, `API-01D`, `API-01E` | Each reusable library seam owns a separate typed-error closure. |
| `DOC-02` | `DOC-02A`, `DOC-02B`, `DOC-02C`, `DOC-02D` | Packet, ingestion, kernel, and daemon public surfaces close independently. |
| `TST-03` | `TST-03A`, `TST-03B`, `TST-03C`, `TST-03D` | Frame, corruption, planner, and parser properties have different generators. |
| `TST-05` | `TST-05A`, `TST-05B`, `TST-05C` | Protocol, MCP, and packet structural snapshots are distinct inventories. |
| `TST-07` | `TST-07A`, `TST-07B` | Ingestion and the packet/kernel/daemon entrypoint families close separately. |
| `TST-08` | `TST-08A`, `TST-08B` | Harness deduplication and RAII process ownership have separate acceptance criteria. |
| `PER-01` | `PER-01A`, `PER-01B` | Task and watch persistence require independently replayable ownership. |
| `PER-03` | `PER-03A`, `PER-03B` | Base-index updates and regex overlays are separate incremental paths. |
| `PER-04` | `PER-04A`, `PER-04B` | Schema initialization and graph workflow connection reuse close separately. |
| `PER-05` | `PER-05A`, `PER-05B` | Sparse storage and sparse planning have distinct parity/performance evidence. |
| `PER-06` | `PER-06A`, `PER-06B`, `PER-06C` | Shared scanning, diagnostics copying, and LCOV allocation are separate mechanisms. |
| `PER-09` | `PER-09A`, `PER-09B`, `PER-09C`, `PER-09D` | Clone, collection, nested merge, and large-by-value claims require separate measurements. |
| `PER-11` | `PER-11A`, `PER-11B`, `PER-11C` | Task count, store bytes, and file-count/size snapshots retain independent scopes. |
| `ASY-01` | `ASY-01A`, `ASY-01B`, `ASY-01C` | Routing, concurrency, and child cleanup are distinct proxy lifecycle contracts. |
| `ASY-02` | `ASY-02A`, `ASY-02B` | Transport task ownership and unified shutdown are independently testable. |
| `ASY-03` | `ASY-03A`, `ASY-03B` | Polling replacement and debounce ownership have separate timer/signal evidence. |
| `ASY-04` | `ASY-04A`, `ASY-04B`, `ASY-04C` | Blocking-work classification, bounded CPU ownership, and persistence placement are distinct constraints. |
| `EXP-02` | `EXP-02A`, `EXP-02B`, `EXP-02C` | Passthrough, stable, and adaptive modes remain separate experimental variants. |
| `EXP-03` | `EXP-03A`, `EXP-03B`, `EXP-03C`, `EXP-03D`, `EXP-03E`, `EXP-03F` | Each required experiment scenario remains individually accountable. |
| `EXP-04` | `EXP-04A`, `EXP-04B`, `EXP-04C`, `EXP-04D`, `EXP-04E` | Churn, reuse, effective cost, rewarm, and provider/adherence evidence are distinct metrics. |
| `INV-04` | `INV-04A`, `INV-04B` | Borrowing and dynamic-dispatch strengths are independent preservation constraints. |
| `INV-06` | `INV-06A`, `INV-06B`, `INV-06C`, `INV-06D`, `INV-06E` | Hash/schema, routes, scheduler, index, and fallback determinism remain separate invariants. |
<!-- END: LEGACY-AUDIT-ID-ALIASES -->

## Follow-up labels that are not baseline audit observations

These labels came from later remediation coordination. They must not be counted
as recovered HTML observations or used to rewrite baseline facts.

| Follow-up label | Scope | Snapshot boundary | Related source-derived rows | Evidence / commit | State |
|---|---|---|---|---|---|
| REP-14 | Cargo publication policy: publishability, internal dependency/version policy, package metadata/artifacts, and release preflight. | Committed at `937876a`. | REP-07, REP-10 | Policy script/tests and `build(release): enforce Cargo publication policy`. | `937876a`; committed follow-up |
| REP-17 | Direct-minimum dependency graph. | Committed after provisional snapshot `1d7b981`; it does not rewrite the baseline observations. | REP-01, REP-02, REP-04, REP-06, INV-01 | Cargo's pinned `-Z direct-minimal-versions` resolver produces `Cargo.direct-minimal.lock`; a manifest digest prevents drift, one documented transitive compatibility pin is enforced mechanically, and every workspace target/feature compiles against the alternate graph. | `f3257090`; committed follow-up |
| REP-18 | YAML parser migration. | Committed and inspected before provisional snapshot `1d7b981`. | REP-04/05B and parser test rows; it does not change their already-closed baseline status. | Typed YAML parse/validation behavior and setup/doctor compatibility tests. | `bbbf2aa`; committed follow-up |
| A2 | Durable search-index clear/retry state. | Committed at `937876a`. | PER-03A, TST-03B, INV-05, INV-06E; supports COR-06A/08B | Typed durable clear/retry implementation and regressions. | `32c82da`; committed follow-up |
| THIN-P28 | Thin p28 process bootstrap. | Committed at `937876a`. | EXT-07 only as pattern evidence. It does **not** close ARC-04A/B. | `refactor(p28): isolate process bootstrap`. | `64ea03f`; committed follow-up |
| RELEASE-SMOKE | Staged platform-package smoke before publication. | Committed at `937876a`. | REP-07, REP-10 | Offline staged package/binary/header/mode/QEMU smoke. | `66ab4ec`; committed follow-up |
| REDUCER-PANIC | Remove production env-parser panic path. | Committed at `937876a`. | REP-05B | Parser regression and strict panic policy. | `6cbe671`; committed follow-up |

## Generated integration records

Do not hand-edit content between generated markers. The integration owner
replaces it after all source changes and final evidence land on the intended
clean tree. The normal ledger checker deliberately permits provisional records
so it remains useful throughout remediation. Finalization is an explicit,
strict check against a caller-selected source revision:

```text
python3 scripts/check_architecture_audit_ledger.py --final --source-rev <rev>
```

A tracked ledger cannot contain the commit or tree hash of the commit that
contains the ledger itself. To make final validation rerunnable, land the
generated ledger synchronization as a ledger-only commit and select its source
parent, normally `--source-rev HEAD^`. The strict checker resolves that revision
and requires the recorded integration commit/tree and all required final gates
to be complete and exact.

<!-- BEGIN GENERATED: LEDGER-SNAPSHOT -->
| Field | Value |
|---|---|
| Provisional source snapshot commit | `1d7b98137e97ba670e1c26f5e3b428ce9d912928` |
| Provisional source snapshot tree | `7b774937f5a5e1389e52699f9df691071f7c8d67` |
| Audit source SHA-256 | `126de8fc65b42bf5000ad1744293a1507e4abf18c1cbabc57dbb9e1a217195a9` |
| Integration commit | `PENDING — generated after the patch lands` |
| Integration tree | `PENDING — generated after the patch lands` |
| Later integration work | Broker/daemon, README/docs taxonomy, evidence recapture, and final-gate commits are deliberately not folded into this provisional snapshot. |
| Dirty-tree fingerprint | `PENDING — final evidence requires a clean tree` |
| Toolchain / host / date | `PENDING — generated with final gate` |
| Commits since original ledger `d12943d` at provisional source snapshot | `117` |
<!-- END GENERATED: LEDGER-SNAPSHOT -->

### Follow-up commit placeholders

<!-- BEGIN GENERATED: FOLLOW-UP-COMMITS -->
| Label | Commit | Focused evidence | Row/status changes authorized |
|---|---|---|---|
| REP-17 direct-minimum dependency graph | `f3257090` | `python3 scripts/validate_direct_minimum.py --refresh --offline`; `python3 scripts/validate_direct_minimum.py --offline`; 39 focused policy/unit tests; workspace check and strict Clippy on the production lock | Records the committed follow-up without changing any baseline source row. |
| REP-18 YAML parser migration | `bbbf2aa` | Typed YAML parse/validation plus setup/doctor compatibility tests; final gate still pending. | No baseline source row changes; follow-up is recorded as committed. |
| Ledger integration | `PENDING` | `python3 scripts/check_architecture_audit_ledger.py` plus 11 mutation tests | Documentation trace only; final integration sync still required. |
<!-- END GENERATED: FOLLOW-UP-COMMITS -->

### Canonical final gate

Every result must name the exact integration commit/tree and an artifact or
captured output. Focused evidence in the rows above is not a substitute.

<!-- BEGIN GENERATED: FINAL-GATE -->
| Gate | Exact command | Result | Artifact / timestamp |
|---|---|---|---|
| Ledger/source-anchor validation | `python3 scripts/check_architecture_audit_ledger.py` | **PASS at provisional snapshot** | 176 unique source rows, 34 legacy aliases, 10 source relationships; exact audit digest/coordinates/schema |
| README generated statistics | `python3 scripts/verify_readme_stats.py` | **KNOWN FAIL at provisional snapshot** | README 30 crates/162,326 lines/578 files vs generated 32/227,205/643 |
| Formatting | `PENDING` | PENDING | PENDING |
| Workspace check | `PENDING` | PENDING | PENDING |
| Workspace build | `PENDING` | PENDING | PENDING |
| Strict Clippy | `PENDING` | PENDING | PENDING |
| Full all-feature tests | `PENDING` | PENDING | PENDING |
| Doctests | `PENDING` | PENDING | PENDING |
| Strict rustdoc | `PENDING` | PENDING | PENDING |
| Architecture rules | `PENDING` | PENDING | PENDING |
| Supply-chain policy | `PENDING` | PENDING | PENDING |
| Exact MSRV | `PENDING` | PENDING | PENDING |
| Packaging/release dry-run | `PENDING` | PENDING | PENDING |
| Cargo publication policy | `PENDING` | PENDING | PENDING |
| Performance/cache experiments | `PENDING` | PENDING | PENDING |
| Runtime-starvation evidence | `PENDING` | PENDING | PENDING |
| Current-source index benchmark | `PENDING` | PENDING | PENDING |
<!-- END GENERATED: FINAL-GATE -->

### Generated row summary

<!-- BEGIN GENERATED: ROW-SUMMARY -->
| Metric | Value |
|---|---|
| Source-derived observation rows | `176` |
| Provenance/verdict/score rows | `27` |
| Remediation rows | `117` |
| Preservation rows | `11` |
| Sequencing rows | `11` |
| External-reference rows | `10` |
| Follow-up-only rows | `7` |
| Legacy aggregate-ID aliases | `34` |
| Explicit source alias/facet relationships | `10` |
| Exact source-coordinate map SHA-256 | `444d943ffaea56121d307b6ecd97ca0a35d962d186b78ef3e930f6c751f8aa22` |
| Rows with `PENDING`, `PARTIAL`, or `REGRESSED` status | `13` |
| Rows missing a source coordinate | `0` |
<!-- END GENERATED: ROW-SUMMARY -->

## Post-remediation blind-review checklist

Two reviewers with no implementer context inspected the exact frozen source
snapshot `14dc5caa63287a3cabbfa403f0beb0640139c288` after the first remediation
series landed. Reviewer A and reviewer B worked independently and made no
source edits. Neither found a P0. The table below deduplicates every actionable
finding from both reports; compound findings are split where they require
different invariants, tests, or commits.

The validation column records only what has been established against current
source. `CONFIRMED` does not mean fixed. A row becomes `DONE` only after its
implementation and mechanically verified invariant are named together with a
reachable closing commit.

| ID | Blind-review source | Current-source validation | Implementation | Required mechanical evidence | Closing commit / state |
|---|---|---|---|---|---|
| BR-01 | B1 | `CONFIRMED` at `14dc5ca`: Unix clients authenticate to the daemon, but do not authenticate the server, and the predictable shared socket parent is not owner/mode validated. | Per-EUID owner-only runtime directories, no-follow/ACL-authenticated runtime discovery, and Unix peer-credential verification now precede the first frame; TCP retains its capability prelude. | `packet28-daemon-client` 21/21 tests cover symlink/FIFO/hard-link/oversize/permission/ancestry rejection, direct and inherited ACL rejection without mutation, and owner-versus-substituted-peer plus retained-descriptor identity changes; integrated client E2Es pass. | `40e1c8cb`, `67c7fef8`, `1ddb7b17`; `DONE` |
| BR-02 | B2 | `CONFIRMED` at `14dc5ca`: `p28` and both instruction shims bypass the authoritative runtime endpoint used by suite-cli. | `packet28-daemon-client` now owns authoritative endpoint discovery and authenticated Unix/TCP/workspace fallback for suite-cli, `p28`, and both shims. | Real-process `p28` forced-TCP and authoritative workspace-fallback E2Es pass, as do five shim unit tests and the native macOS interpose test; the Linux preload test is committed but cannot execute on this macOS host. | `67c7fef8`; implementation complete, native Linux preload gate pending (`PARTIAL`) |
| BR-03 | A1, B3 | `CONFIRMED` at `14dc5ca`: a delegated child can execute after spawn but before durable ownership publication. | A pipe-backed launch gate now releases delegated work only after synchronous ownership checkpoint and durable start-event publication. | `crash_between_spawn_and_ownership_checkpoint_never_releases_delegated_work`; integrated daemon lifecycle suite 6/6. | `a58f58c8`; `DONE` |
| BR-04 | B3 | `CONFIRMED` at `14dc5ca`: durable task state represents only the latest child while the runtime permits overlapping children. | A per-task-generation RAII permit rejects overlap before spawning a second child. | `active_delegated_launch_rejects_overlap_for_the_same_task`; integrated daemon lifecycle suite 6/6. | `30f71c77`; `DONE` |
| BR-05 | A4 | `CONFIRMED` at `14dc5ca`: the waiter can emit `task.agent_launch_completed` before the caller emits `task.agent_launch_started`. | The durable started event is published before the launch gate lets the child exit. | `immediate_delegated_exit_records_started_before_completed`; integrated daemon lifecycle suite 6/6. | `a58f58c8`; `DONE` |
| BR-06 | B4 | `CONFIRMED` at `14dc5ca`: the macOS swap child can execute between spawn and durable PID/PGID/start-time publication. | A socket-backed launch gate retains PID/PGID identity across `exec` and releases the target only after the authenticated `Active` journal is durable; recovery fails closed if orphan termination fails. | Integrated `cmd_macos_swap` suite 46/46 and native macOS runtime-backend E2E 4/4, including hard exit at `AfterSpawn` and active-journal write failure. | `62c8d0d5`; `DONE` |
| BR-07 | B5 | `CONFIRMED` at `14dc5ca`: split task/watch publication detects a torn pair but overwrites the only recoverable prior watch half. | Registry publication now persists authenticated prior images and a recovery journal before replacing either canonical half; one atomic manifest is the commit point, retry first heals the recoverable base, and startup resolves the committed pair before readiness. | Abrupt first-half exit, corrupt recovery image/manifest, retry-after-interruption, retention post-rename failure, and real daemon-restart regressions pass; daemon-core 357 tests and startup 3/3 pass on the integrated tree. | `0623c191`; `DONE` |
| BR-08 | B6 | `CONFIRMED` at `14dc5ca`: a valid 5,000-task registry serializes an 8,550,193-byte `Status`, exceeding the 8,388,608-byte frame cap. | A separate Registry V1 protocol keeps compact status bounded and pages task/watch detail with byte/item caps, exclusive cursors, revision-plus-instance snapshot tokens, and unknown-variant-only legacy fallback over the authenticated connection. | The process fixture reproduces an 8,570,193-byte legacy response and returns 5,000/5,000 tasks at one revision in 831.74 ms; protocol 59 tests, three doctests, registry 18/18, startup 3/3, strict relevant Clippy, and architecture gates pass. | `aa66584a`; `DONE` |
| BR-09 | B7 | `CONFIRMED` at `14dc5ca` with a live `.packet28 -> victim` reproduction. | The shared `packet28-state-fs` capability anchors repository-state ancestry and revalidates retained identities; context checkpoint/WAL/lock files, map cache/generations, and search layers use its no-follow publication and admission APIs. | Symlink-parent, planted-leaf, post-open replacement, and ancestor-swap regressions leave victim bytes unchanged across state-fs, context, map, and search; the integrated four-package suite and strict Clippy pass. | `f2b29f95`, `3e3ceef4`, `b64afbe5`, `d4b757ef`; `DONE` |
| BR-10 | B8 | `CONFIRMED` at `14dc5ca` with a FIFO checkpoint hang; checkpoint and WAL loaders read before type/size admission. | State inputs must be admitted as regular files and read through hard byte ceilings before allocation or mapping; context persistence also enforces bounded child-process resource limits. | FIFO, symlink, oversized sparse checkpoint/WAL/metadata/mmap, and replacement tests pass under bounded resource/deadline fixtures; state-fs 4/4, context 65/65 plus wire, map 70/70, and search 125 focused tests/doctests pass. | `f2b29f95`, `3e3ceef4`, `b64afbe5`, `d4b757ef`; `DONE` |
| BR-11 | B9 | `CONFIRMED` at `14dc5ca`: missing-path suffix traversal recursively follows directory symlinks without visited-set or traversal budgets. | Path inspection rejects symlink ancestors; the isolated suffix resolver skips symlinks and root control directories, deduplicates canonical directories, and fails closed after 64 levels or 100,000 entries. | Four traversal unit regressions cover symlink cycles, control state, depth exhaustion, and entry exhaustion; two public-API regressions cover external symlinks; search-core 85 unit tests, 12 generation tests, 4 architecture tests, 15 public-API tests, doctests, and strict Clippy pass. | `654682fb`, `8cf9651d`; `DONE` |
| BR-12 | B10 | `CONFIRMED` at `14dc5ca`: reservation and eviction use different timestamps, and a persistence rejection is swallowed after acknowledged mutation. | One persistence owner now samples the live timestamp once, pre-encodes and bounds WAL frames before visibility, linearizes reserved revisions, publishes only accepted mutations, propagates failures, and invokes caller hooks after releasing the cache lock. | Advancing-clock TTL, rejection-before-visibility, same-key ordering, WAL chunking, re-entrant hook, bounded-drop, and live-versus-restart parity regressions pass; memory 82 plus wire, mechanism 5 plus sequence/wire, builtins 57, strict focused Clippy/rustdoc, and PER-02 verification pass on the integrated tree. | `64ba2c03`, evidence `011e89bb`; `DONE` |
| BR-13 | B10 related P2 | `CONFIRMED` at `14dc5ca`: checkpoint expiry filtering persists an average computed from the unfiltered documents. | Checkpoint persistence now derives document count, length total, average length, and indexes from the same expiry-filtered live document set. | Expiry-boundary checkpoint, first-insert live/restart scoring parity, shutdown/reload, and aggregate reconstruction regressions pass within the integrated 82-test memory suite and verified PER-02 artifact. | `f72cfa69`, evidence `011e89bb`; `DONE` |
| BR-14 | B11 | `CONFIRMED` at `14dc5ca`: stable dirty full builds publish ready with no usable clean-commit or overlay attestation. | Full rebuild now authenticates the final Git/workspace snapshot and rejects dirty publication while retaining the prior ready generation; empty `.git` markers retain non-Git compatibility. | `full_rebuild_rejects_a_dirty_git_workspace_without_replacing_the_ready_generation` plus the full search-core and CLI daemon E2E suites pass. | `6cdce36c`, `767fcce7`; `DONE` |
| BR-15 | A2, B12 | `CONFIRMED` at `14dc5ca`: one indexed daemon query takes three repository snapshots/six Git processes; `Command::output` buffers before a 32 MiB post-check and has no timeout. | Git metadata is streamed through a hard byte cap and timeout; a routed query carries one retained authenticated attestation instead of recomputing it. | Hanging/oversized fake-Git tests and `guarded_query_runs_one_workspace_attestation` pass. Criterion measured cached combined attestation at 15.5–18.0 ms versus 33.1–39.5 ms split, and from-disk combined at 17.3–18.9 ms versus 47.5–62.0 ms split. | `b5d97d7d`, `93075e37`, `16d70c6d`; `DONE` |
| BR-16 | B13 | `CONFIRMED` at `14dc5ca`: before/after Git state can return to the same commit while transient scanned bytes are published. | Full-build publication binds scanned content fingerprints to the final authenticated clean tree/workspace snapshot and rejects a restored-commit ABA. | `full_rebuild_rejects_clean_workspace_aba_bytes`; full search-core all-feature suite and strict Clippy pass. | `6cdce36c`; `DONE` |
| BR-17 | B14 | `CONFIRMED` at `14dc5ca`: a one-file map update reloads, hashes, and deserializes the full base plus segments; the current benchmark is about 3.4–3.5× slower than legacy and masks regressions with `saturating_sub`. | One authenticated publication transaction now covers stable increments, direct/policy rebuilds, and prepared/shared publication. On Unix, every writer first acquires an advisory lease on an independently opened handle for the retained `.packet28/index` directory and holds it through leaf unlock; the conventional writer-lock leaf and its attachment checks remain defense-in-depth. This stable parent lease serializes cooperative successors even if the leaf is renamed after its last pre-mutation check. The transaction authenticates raw prestate and owned outputs before replacement; refuses stale rollback/prune/clear after lock detachment; pins N/N+1 records and every referenced artifact; and bounds runtime-map allocation to a validated private generation rather than caller-mutable manifest fields. Stable Unix identity avoids unchanged-artifact rehashing while new segments and policy bases report their actual digest, persisted-byte authentication, and decode work. | Deterministic split-lock regressions replace the leaf immediately before the atomic current-manifest rename and immediately before descriptor-relative clear removal, prove the replacement leaf alone is lockable while the retained parent is contended, then prove the blocked successor publishes intact after the stale owner exits. Additional detachment cases cover before current, after current, after previous, prepared rollback, and prune. Runtime policy/count/generation/digest/status tampering returns typed cache errors before allocation. Three release runs report 2,711 metadata bytes decoded, one 3,439-byte segment decoded, 6,878 artifact bytes hashed, 42 checks, and one changed path. The corrected complete `.packet28/index` byte scope includes the 44-byte generation-high-water change: 5,367 versus 3,490,797 bytes (99.85% reduction). Incremental medians are 63,556–77,221 µs versus 4,627–5,583 µs for the non-durable comparator, so the elapsed-time objective remains experiment-gated pending safe durability-barrier coalescing. | `a9e9c5f6`, `a150f514`, `252c8dbe`, `161ea15a`, `72a53bea`; current repair worktree; `PARTIAL` |
| BR-18 | B14 | `CONFIRMED` at `14dc5ca`: production map and symbol/pattern consumers rescan instead of consuming the daemon's persisted runtime. | Broker map/symbol consumers build directly from authenticated daemon runtimes. Pattern candidates use a trusted internal batch session with one cumulative 64-file verification budget: zero/overbroad sets preserve fallback, unused phase two never executes, every read binds path/descriptor/size/BLAKE3 fingerprint to the active indexed document, and no result batch is exposed before its final workspace attestation. A selected fallback therefore performs a second fully attested batch before the current-runtime gate; an unused fallback performs only the primary attestation. Empty and root/readiness/dirty/queued mismatches fail closed without rescanning. | Incremental-runtime E2E proves both consumers observe one-file publication. The persisted-corruption E2E reloads corrupt manifests and renames the source tree so a rescan is impossible. Search-core regressions cover the absolute/cumulative ceilings, bounded one-time requested-scope resolution, zero candidates, empty-batch validation, unused phase two, source mutation, symlink/ABA substitution, sparse high-line evidence, and one two-command Git attestation per exposed batch; the architecture mutation suite rejects rescan, shared-scan ownership, runtime rebuild/load/update aliases, and unbatched indexed-query entrypoints. | `6a62d826`, `801a581b`, `95cbaba3`; `DONE` |
| BR-19 | A5 | `CONFIRMED` at `14dc5ca`: MCP overload emits one `id:null` response instead of preserving request IDs and batch semantics. | Overload handling now emits one error per request with its original typed ID, preserves batch shape, and emits nothing for notifications or client responses. | Saturated single and mixed-batch E2E plus unit regressions for notification-only and partial-notification batches pass. | `b1646f5d`, `6393d5c4`; `DONE` |
| BR-20 | A6 | `CONFIRMED` at `14dc5ca`: predictable proxy-generated upstream IDs can collide with forwarded client IDs. | Internal upstream requests use an upstream-scoped namespace and the tracker restores the original typed client ID on the response path. | Concurrent reverse-request routing plus explicit string/numeric ID-type and translation regressions pass. | `75cabf61`, `8875c463`; `DONE` |
| BR-21 | B15 | `CONFIRMED` at `14dc5ca`: upstream timeout/disconnect can leave a durable MCP invocation at `Started`. | Every transport error after durable start now writes a terminal failure with the same invocation identity and chains any terminal-write failure into the returned error. | Timeout and disconnect E2Es inspect raw persisted agent-state events and assert matching start/failure ID, sequence, server, fingerprint, error class, retryability, duration, and message. | `86221a1b`, `ead73168`; `DONE` |
| BR-22 | B16 | `CONFIRMED` at `14dc5ca`: resource-template ownership is not retained, and duplicate static URIs are advertised while last-owner routing wins. | One catalog now retains exact and conservative RFC 6570 Level-1 template owners, resolves their full owner union, rejects deterministic ambiguity instead of selecting the last owner, exhausts bounded upstream pages, serves immutable bounded downstream snapshots, and epoch-invalidates dynamic routes before forwarding `list_changed`. | Routing/pagination/epoch unit groups pass; the rebased serial proxy suite passes 15/15, including exact/template ambiguity, page-two routing, cursor cycles, stable downstream paging, dynamic invalidation, and original typed IDs; strict all-target Clippy passes. | `f955e0dd`, `e5eef405`, `c7a1fae4`; `DONE` |
| BR-23 | B17 | `CONFIRMED` at `14dc5ca`: one notification log/delivery error terminates the poller without failing or supervising the MCP session. | Native and proxy serving now supervise notification completion across stdin, initialization, dispatch, output, and concurrent request lifetimes; a poller error or panic fails the serving session immediately and bounded teardown cancels blocked writers/upstreams. | Notification error/panic/cancellation/abort unit regressions pass; native stdio E2E 3/3 and proxy E2E 15/15 cover initialization, tool-call, and stdout-backpressure failure through the bounded process owner while strict all-target Clippy passes. | `e840a542`, harness ownership `c6bc995c`; `DONE` |
| BR-24 | A3 | `CONFIRMED` at `14dc5ca`: release packaging and its verifier omit documented `packet28-agent` and Linux `libcontext_instruct_shim.so`. | One authoritative artifact contract now drives CI, local staging, npm verification, and root wrappers; Linux packages include the shim. | 12 release-verifier tests, 27 CI-policy tests, five offline npm pack/publish dry-runs, all four executable checks, and a required native-Linux preload smoke. | `b5e27563`; implementation complete, native Linux CI execution pending |
| BR-25 | A7 | `CONFIRMED` at `14dc5ca`: README claims 30 crates and 162,326 lines/578 Rust files; generated values are 32 and 247,131/657. | Replace the README and source volatile statistics from the generator. | `python3 scripts/verify_readme_stats.py --check`. | `PENDING` |
| BR-26 | A8 | `CONFIRMED` at `14dc5ca`: the workspace-policy checker hard-codes 23 internal dependencies and omits two current workspace crates. | Cargo metadata now derives the 25 internal dependencies and enforces local paths plus bounded requirements. | 16 publish-policy tests mutate every discovered internal dependency; workspace policy passes for 32 members and 25 dependencies. | `589b16fc`; `DONE` |
