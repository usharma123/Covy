# Packet28 RTK/ICM Parity Audit

This file is the source-backed parity contract for Packet28 against current upstream RTK and ICM.

## Source Snapshot

| Source | Revision inspected | Local path | Evidence used |
|---|---:|---|---|
| RTK | `55f998d` | `/private/tmp/rtk-parity` | `README.md`, `hooks/README.md`, `src/main.rs`, `src/discover/registry.rs`, `src/discover/rules.rs`, `src/hooks/*`, `src/core/tracking.rs` |
| ICM | `92bd851` | `/private/tmp/icm-parity` | `README.md`, `crates/icm-cli/src/main.rs`, `crates/icm-mcp/src/tools.rs`, `crates/icm-store/src/schema.rs`, `crates/icm-core/src/*` |
| Packet28 | current workspace | `/Users/utsavsharma/Documents/GitHub/Coverage` | `crates/suite-cli/src/cli_defs.rs`, `cmd_run.rs`, `cmd_compact.rs`, `route_registry.rs`, `cmd_memory.rs`, `memory_store.rs`, `cmd_feedback.rs`, `cmd_graph.rs`, `cmd_wakeup.rs`, `cmd_dashboard.rs`, `cmd_mcp*`, `cmd_setup.rs`, `cmd_doctor.rs` |

Status values:

- `implemented`: Packet28 has a working Packet28-native equivalent.
- `partial`: Packet28 has a useful subset but not full upstream parity.
- `missing`: Packet28 does not yet have the capability.
- `non-goal`: Packet28 intentionally should not implement the upstream behavior.

## Current Conclusion

Packet28 is useful today as a local context-reduction, search, runtime-hook, MCP, and lightweight memory helper. It is not yet at full upstream RTK + ICM parity.

The largest gaps are:

- RTK: broader reducer/filter catalog, custom TOML filters, telemetry controls if deliberately adopted, full agent breadth including Copilot/Gemini/OpenCode/Cline/Roo/Kilo/Antigravity/Hermes, and hook-level transparent rewrite proof across every claimed agent.
- ICM: transcript runtime breadth, web/TUI dashboard parity, and broader real-runtime evidence for the local memory/graph/feedback/wake-up loop.
- Evidence: Packet28 now has an initial repeated real-repo smoke artifact across Packet28, ripgrep, and fd with stable indexed `p28` hits, but broader task-shape coverage is still needed before maturity claims are honest.

## Experiment Evidence

- `docs/experiments/real-repos/SMOKE_20260511.md` records three repeated runs across Packet28, ripgrep, and fd for search, code review, failing-test triage, implementation-state, docs lookup, and handoff/bootstrap workflows. The run produced 54 native runs, 54 `Packet28 run --json` runs, 0 Packet28 fallback reasons, 0 failed commands, and 54/54 raw artifacts available.
- The same artifact records `p28` indexed-search attempts separately: 9/9 indexed-search hits and 0 readiness fallbacks after `p28` was changed to wait for daemon index readiness before falling back.
- `docs/experiments/real-repos/SMOKE_20260511.jsonl` contains the compact per-command metrics used by the summary. Raw stdout/stderr captures remain ignored under the generated run directory.
- `docs/experiments/search-backends/FFF_EVAL_20260511.md` records source inspection of `fff` (`dmtrKovalenko/fff` at `39f67c0`) as a candidate future search backend. Packet28 now has an opt-in `p28 --engine fff` adapter that calls external `fff-mcp` over MCP and converts `grep` output into Packet28 `SearchResult` packets with `engine=fff_mcp`; local tests and a real upstream `fff-mcp` release smoke passed. `docs/experiments/search-backends/FFF_COMPARE_20260511/summary.md` records 81 repeated rows across native `rg`, default `p28`, and `p28 --engine fff`: 0 failed rows, 27/27 `fff_mcp` adapter rows, 15/27 default indexed rows, and 12/27 default legacy fallbacks for broad/common-literal searches. Packet28 `p28 --engine auto` selects `fff-mcp` when the indexed planner would otherwise fall back to broad/common legacy grep and `fff-mcp` is available; `P28_FFF_AUTO=prefer` makes auto mode try `fff-mcp` first before falling back to Packet28's native indexed/reducer path. Full search-backend adoption still needs broader artifacts proving daemon integration, raw artifact recovery, fallback provenance, and whole-agent parity.
- Top-level RTK command-family wrapper evidence now includes `Packet28 npm`, `Packet28 npx`, `Packet28 tsc`, `Packet28 vitest`, `Packet28 pytest`, `Packet28 ruff`, `Packet28 wc`, `Packet28 wget`, `Packet28 curl`, `Packet28 docker`, `Packet28 kubectl`, `Packet28 cargo`, `Packet28 go`, `Packet28 dotnet`, `Packet28 golangci-lint`, `Packet28 gradlew`, `Packet28 rake`, `Packet28 rspec`, `Packet28 rubocop`, `Packet28 mypy`, `Packet28 pip`, and `Packet28 pnpm`. Current evidence: `test_system_language_tool_commands_wrap_common_rtk_tools`, `test_system_infra_and_count_commands_use_reducer_wrappers`, and `test_system_build_and_lint_commands_use_reducer_wrappers`.

## RTK Parity Matrix

| RTK capability | RTK command/API | Upstream evidence | Packet28 equivalent | Status | Test or evidence needed |
|---|---|---|---|---|---|
| CLI proxy command families | `rtk <command-family>` | `src/main.rs` defines reducers for `ls`, `tree`, `read`, `smart`, `git`, `gh`, `glab`, `aws`, `psql`, `pnpm`, `err`, `test`, `json`, `deps`, `env`, `prettier`, `format`, `npm`, `npx`, `tsc`, `vitest`, `pytest`, `ruff`, plus cargo/go/js/python/ruby/dotnet/cloud modules; `src/cmds/system/read.rs` applies optional language-aware filtering, line windows, tail windows, and line numbers; `src/cmds/system/summary.rs` runs commands and detects test, build, log, list, JSON, or generic output for compact summaries; `src/cmds/system/find_cmd.rs` supports compact and native find syntax for name, type, maxdepth, and case-insensitive matches while rejecting compound actions; `src/cmds/system/grep_cmd.rs` groups matches by file, truncates long lines, supports context-only snippets and file-type filters; `src/cmds/system/json_cmd.rs` compacts JSON or renders schema-only output while rejecting known non-JSON extensions; `src/cmds/system/deps.rs` summarizes Cargo, package.json, requirements.txt, pyproject.toml, and go.mod manifests; `src/cmds/system/env_cmd.rs` categorizes environment variables and masks key/secret/password/token-style names; `src/cmds/system/log_cmd.rs` normalizes timestamps, UUIDs, hex values, large numbers, and paths to deduplicate repeated error/warn/info log lines; `src/cmds/js/prettier_cmd.rs` filters Prettier format output and `src/cmds/system/format_cmd.rs` dispatches prettier/black/ruff/biome; `src/cmds/ruby/rake_cmd.rs` filters Minitest output for `rake test` and `rails test`; `src/cmds/git/gt_cmd.rs` filters Graphite `gt log/submit/sync/restack/create/branch` output and delegates `gt status/diff/show/...` to git reducers; `src/cmds/jvm/gradlew_cmd.rs` filters Gradle build/test/connected-test/lint/dependencies output and bypasses verbose stacktrace/info/debug modes; `src/cmds/cloud/aws_cmd.rs` defines specialized AWS reducers for STS, S3/S3API, EC2, ECS, RDS, CloudFormation, CloudWatch Logs, Lambda, IAM, DynamoDB, EKS, and SQS; `src/cmds/system/wc_cmd.rs` compacts `wc` line/word/byte counts by stripping padding and paths; `src/cmds/cloud/wget_cmd.rs` strips wget progress output and summarizes saved file/stdout/error cases. | `Packet28 proxy run`, `Packet28 run`, `Packet28 rewrite`, reducer crates for git including Graphite `gt log/submit/sync/restack/create` and `gt status` delegation, rust, JS including Prettier/npm/npx/tsc/vitest, Python including pytest/ruff and `ruff format`, Go, Ruby including `rake test`, .NET, JVM/Gradle, search, fs including `tree` and `wc`, infra including `psql`, `wget`, and AWS service/action reducers, GitHub/GitLab CLI, plus top-level `Packet28 read`, `Packet28 smart`, `Packet28 summary`, `Packet28 err`, `Packet28 prettier`, `Packet28 format`, `Packet28 npm`, `Packet28 npx`, `Packet28 tsc`, `Packet28 vitest`, `Packet28 pytest`, `Packet28 ruff`, `Packet28 wc`, `Packet28 wget`, `Packet28 curl`, `Packet28 docker`, `Packet28 kubectl`, `Packet28 cargo`, `Packet28 go`, `Packet28 dotnet`, `Packet28 golangci-lint`, `Packet28 gradlew`, `Packet28 rake`, `Packet28 rspec`, `Packet28 rubocop`, `Packet28 mypy`, `Packet28 pip`, `Packet28 pnpm`, `Packet28 find`, `Packet28 grep`, `Packet28 json`, `Packet28 deps`, `Packet28 env`, and `Packet28 log` system commands with optional JSON envelopes. `Packet28 grep --engine fff` delegates to the existing `p28 --engine fff` / `fff-mcp` adapter instead of introducing a second fff integration. | partial | Matrix every RTK command family to Packet28 reducer support and add missing reducer tests. Current added evidence: `read_filters_comments_and_applies_line_windows`, `read_line_numbers_are_stable_width`, `test_system_read_command_filters_and_numbers_files`, `smart_summarizes_source_file_with_local_heuristics`, `test_system_smart_command_summarizes_source_file`, `test_system_err_command_preserves_exit_and_summarizes_failure`, `test_system_prettier_and_format_commands_wrap_formatter_output`, `test_system_language_tool_commands_wrap_common_rtk_tools`, `test_system_infra_and_count_commands_use_reducer_wrappers`, `test_system_build_and_lint_commands_use_reducer_wrappers`, `summary_detects_json_and_test_output`, `test_system_summary_command_preserves_exit_and_summarizes_output`, `find_parses_native_and_compact_args`, `find_renders_limited_matches`, `test_system_find_command_supports_native_find_shape`, `grep_groups_matches_and_honors_file_type`, `test_system_grep_command_groups_matches`, `test_system_grep_fff_engine_delegates_to_p28`, `json_compact_truncates_values_and_summarizes_arrays`, `json_schema_renders_types_without_values`, `json_extension_rejects_cargo_toml_with_deps_hint`, `deps_summarizes_multiple_manifest_types`, `env_masks_sensitive_values_and_limits_path`, `env_filter_includes_other_matching_vars`, `log_analysis_normalizes_variable_ids_and_paths`, `test_system_json_deps_and_env_commands`, `test_system_log_command_deduplicates_noisy_lines`, `reduce_tree_summarizes_footer_counts`, `test_run_reduces_tree_command`, `reduce_wc_single_file_strips_path_and_padding`, `reduce_wc_lines_only_returns_count`, `reduce_wc_multi_file_strips_common_prefix`, `reduce_wget_summarizes_saved_file`, `reduce_wget_stdout_compacts_first_lines`, `reduce_wget_error_extracts_http_status`, `classify_gt_status_delegates_to_git_status`, `reduce_gt_log_strips_email_and_compacts_graph`, `reduce_gt_submit_summarizes_pushed_branches_and_prs`, `reduce_gt_sync_restack_and_create_use_short_confirmations`, `classify_gradle_tasks_and_declines_verbose_modes`, `reduce_gradle_build_keeps_status_warnings_and_errors`, `reduce_gradle_test_keeps_failure_and_user_frame`, `reduce_gradle_connected_reports_missing_device`, `reduce_gradle_lint_keeps_violation_context`, `reduce_gradle_dependencies_summarizes_top_level_configs`, `classify_rake_test_and_compact_minitest_failures`, `classify_bundle_exec_rake_test`, `reduce_glab_mr_list_summarizes_entries`, `reduce_psql_table_summarizes_and_compacts_rows`, `reduce_aws_sts_summarizes_identity`, `reduce_aws_ec2_instances_summarizes_nested_reservations`, `reduce_aws_s3api_objects_summarizes_size`, `reduce_aws_s3_ls_summarizes_text_output`, `test_run_reduces_file_and_search_commands`, `test_run_reduces_docker_logs_and_gh_pr_checks`, `test_run_reduces_ruby_and_dotnet_commands`, `test_run_raw_artifact_available_across_reducer_families`. |
| Command rewrite registry | `rtk rewrite "<command>"` | `src/discover/registry.rs` and `src/discover/rules.rs` classify supported, ignored, unsupported, compound commands, env prefixes including `sudo` and `env VAR=...`, absolute binary paths, git global opts, `git`/`yadm` aliases, golangci-lint global options before `run`, heredocs, pipes, disabled prefixes, `cargo install`, JavaScript package-runner prefixes such as `npm exec`, `npm x`, `npm run-script`, `pnpm dlx`, `pnpm exec`, and `pnpx`, Python package-manager forms such as `pip3`, `pip outdated`, `pip install`, `pip show`, `uv pip ...`, and `ruff format`, Docker mutation forms `build`/`run`/`exec` plus `compose build`, `kubectl apply`, Swift build/test, and rule statuses. | `Packet28 rewrite <command...>` backed by `route_registry.rs` and compact command planner; route tests cover env prefixes, `sudo` and `env VAR=...` runtime prefixes, absolute binary path normalization, RTK ignored exact/prefix commands, RTK `yadm` git-equivalent routing, golangci-lint global-option normalization before `run`, RTK `cargo install` as a mutating reducer, RTK JavaScript package-runner prefixes for tsc/eslint/biome/next/prisma/vitest/jest/playwright, RTK Python pip/uv package-manager forms for list/outdated/install/show, RTK `ruff format` as a mutating reducer while leaving `--diff` raw, RTK Docker `build`/`compose build`/`run`/`exec` and `kubectl apply` mutation forms, RTK Swift build/test via built-in TOML filters, `PACKET28_DISABLED`/`RTK_DISABLED` bypass, compound `&&`/`||`/`;` commands with mixed supported/unsupported segments, pipe commands that rewrite the left side and resume after chain operators, heredocs, git global opts, unsupported pass-through, glob safety, already-Packet28 commands, and TOML-filter fallback commands routed to `Packet28 run`. | partial | Broaden RTK-rule parity against the full upstream rule table. Current evidence: `route_registry::tests`, including `routes_sudo_and_env_prefixed_commands_like_rtk`, `normalizes_absolute_binary_paths_like_rtk`, `marks_rtk_ignored_commands_as_intentional_passthrough`, `routes_yadm_like_rtk_git_alias`, `routes_golangci_global_options_before_run_like_rtk`, `routes_rtk_cargo_install_as_mutation`, `routes_rtk_javascript_package_runner_prefixes`, `routes_rtk_python_package_manager_forms`, `routes_rtk_ruff_format_as_mutation`, `routes_rtk_docker_and_kubectl_mutation_forms`, `builtin_toml_filters_route_to_packet28_run`, `builds_compound_rewrite_for_supported_and_mixed_segments`, and `builds_pipe_rewrite_for_left_side_only_then_resumes_after_chain_operator`; `classify_yadm_delegates_to_git_reducers`; `classify_and_reduce_cargo_install_as_mutation`; `classify_javascript_accepts_rtk_package_runner_prefixes`; `classify_python_accepts_rtk_pip_package_manager_forms`; `classify_and_reduce_ruff_format_as_mutation`; `classify_infra_supports_rtk_docker_and_kubectl_mutation_forms`; `test_verify_filters_runs_inline_toml_tests` covers `swift-test`; `test_top_level_rewrite_handles_compound_commands_like_rtk`. |
| Runtime hook delegation | agent hooks call `rtk rewrite` | `hooks/README.md` says hook scripts are thin delegates and all rewrite logic lives in Rust registry. | Packet28 setup installs runtime hooks/configs and hook handlers for Packet28-supported runtimes; Claude, Cursor, Copilot, and Gemini PreToolUse-style hook paths delegate through Packet28's shared route/reducer planner and return runtime-specific rewrite JSON, including compound and TOML-filter rewrite routes. OpenCode and Hermes generated plugins call `Packet28 rewrite` directly and fail open on errors; no-rewrite `Packet28 rewrite` text mode emits empty stdout so plugin delegates do not mutate unsupported commands. | partial | Add live smoke against plugin-hosted OpenCode/Hermes runtimes if required. Current evidence: `runtime_pretool_rewrites_use_shared_route_planner`, `pretool_rewrites_supported_compound_command`, `test_top_level_rewrite_prints_empty_stdout_on_no_rewrite`, `opencode_plugin_smoke_rewrites_and_passes_through_empty_stdout`, `hermes_plugin_smoke_rewrites_and_passes_through_empty_stdout`, `test_packet28_hook_pretool_rewrites_supported_git_command`, `test_packet28_cursor_hook_pretool_rewrites_and_returns_empty_json_on_noop`, `test_packet28_gemini_hook_before_tool_rewrites_shell_command`, `test_packet28_copilot_hook_rewrites_vscode_and_denies_cli_with_suggestion`, `cmd_hook::tests::pretool_rewrites_strict_*`. |
| Claude Code transparent rewrite | `rtk init -g` / Claude hook | `hooks/README.md` documents `PreToolUse` updatedInput support. | Packet28 setup/doctor supports Claude Code; Claude `PreToolUse` Bash payloads return `updatedInput` for supported commands, no-op unsupported/non-Bash payloads, and avoid double-rewriting already wrapped Packet28 reducer-runner commands. | partial | Add live smoke against an installed Claude Code runtime if required. Current evidence: `write_claude_hook_config_installs_command_and_http_hooks`, `test_packet28_hook_pretool_rewrites_supported_git_command`, `test_packet28_hook_pretool_is_idempotent_and_ignores_non_bash_tools`, family-specific PreToolUse rewrite tests, `test_packet28_hooks_degrade_gracefully_on_bad_json_and_no_rewrite`. |
| Cursor transparent rewrite | `rtk init -g --agent cursor` | `hooks/README.md` documents `preToolUse` updated_input and `{}` no-op response. | Packet28 setup installs Cursor hook config and `Packet28 hook cursor` handles Cursor `beforeShellExecution` payloads, returns `updated_input` for supported commands, returns `{}` for no-rewrite commands, accepts top-level `command`, nested `tool_input.command`, `command_line`, and `shell_command`, and avoids double-rewriting already wrapped reducer-runner commands. | partial | Add live smoke against an installed Cursor runtime if required. Current evidence: `write_cursor_hook_config_installs_packet28_hooks`, `test_packet28_cursor_hook_pretool_rewrites_and_returns_empty_json_on_noop`. |
| Copilot support | `rtk init -g --copilot` | `README.md`, `hooks/README.md`, `hooks/copilot/README.md`, and `src/hooks/hook_cmd.rs` document VS Code Copilot Chat transparent rewrite via `updatedInput` plus GitHub Copilot CLI deny-with-suggestion for camelCase `toolName`/JSON-string `toolArgs`. | `Packet28 setup --runtime copilot` writes `.github/copilot-instructions.md` plus `.github/hooks/packet28-rewrite.json`; `Packet28 hook copilot` auto-detects VS Code-style `tool_name/tool_input.command` and Copilot CLI `toolName/toolArgs`, returning `hookSpecificOutput.updatedInput` for VS Code and top-level `permissionDecision: deny` with a Packet28 suggestion for Copilot CLI. `Packet28 doctor --agent copilot` verifies both guidance and hook config. | partial | Add live smoke against installed VS Code Copilot/Copilot CLI if required. Current evidence: `write_copilot_hook_config_installs_packet28_pretool_hook`, `test_setup_copilot_writes_instructions_and_pretool_hook`, `test_packet28_copilot_hook_rewrites_vscode_and_denies_cli_with_suggestion`. |
| Gemini support | `rtk init -g --gemini` | `README.md`, `hooks/README.md`, and `src/hooks/init.rs` document a global Gemini `BeforeTool` hook in `~/.gemini/settings.json` for `run_shell_command`; `src/hooks/hook_cmd.rs` returns `{"decision":"allow","hookSpecificOutput":{"tool_input":{"command":"..."}}}` when rewritten and `{"decision":"allow"}` on no-op. | `Packet28 setup --runtime gemini` writes `GEMINI.md` plus a global `~/.gemini/settings.json` `BeforeTool` hook for `run_shell_command`; `Packet28 hook gemini` rewrites supported `run_shell_command` payloads through the shared Packet28 reducer planner with the Gemini `hookSpecificOutput.tool_input.command` contract and allows unsupported/non-shell tools without mutation. | partial | Add live smoke against an installed Gemini CLI runtime if required. Current evidence: `write_gemini_hook_config_installs_packet28_before_tool_hook`, `test_setup_gemini_writes_before_tool_hook_and_prompt`, `test_packet28_gemini_hook_before_tool_rewrites_shell_command`. |
| OpenCode support | `rtk init -g --opencode` | `hooks/opencode/rtk.ts` and `hooks/opencode/README.md` document a TypeScript plugin installed to `~/.config/opencode/plugins/rtk.ts`; it intercepts `tool.execute.before`, calls `rtk rewrite`, and mutates `args.command` in place on rewrite. | `Packet28 setup --runtime opencode` writes `AGENTS.md` plus `~/.config/opencode/plugins/packet28.ts`; the plugin intercepts OpenCode `tool.execute.before` for Bash/shell tools, calls `Packet28 rewrite`, mutates `args.command` only when rewrite stdout is non-empty and different, preserves commands when rewrite stdout is empty, and fails open on errors. `Packet28 doctor --agent opencode` verifies both guidance and plugin install. | partial | Add live smoke against installed OpenCode if required. Current evidence: `write_opencode_plugin_installs_packet28_rewrite_plugin`, `opencode_plugin_smoke_rewrites_and_passes_through_empty_stdout`, `test_setup_opencode_writes_instructions_and_rewrite_plugin`. |
| Hermes support | `rtk init --agent hermes` | `hooks/hermes/rtk-rewrite/__init__.py`, `plugin.yaml`, `README.md`, and `src/hooks/init.rs` document a Python plugin installed under `~/.hermes/plugins/rtk-rewrite/`, enabled through `plugins.enabled`, intercepting Hermes `pre_tool_call` for `terminal` and mutating `args.command` via `rtk rewrite`. | `Packet28 setup --runtime hermes` writes `AGENTS.md`, installs `~/.hermes/plugins/packet28-rewrite/{__init__.py,plugin.yaml}`, and enables `packet28-rewrite` in `~/.hermes/config.yaml`; the plugin intercepts Hermes `pre_tool_call` for `terminal`, calls `Packet28 rewrite`, mutates `args.command` only when rewrite stdout is non-empty and different, preserves commands when rewrite stdout is empty, and fails open on timeout/error. `Packet28 doctor --agent hermes` verifies guidance, plugin files, and config enablement. | partial | Add live smoke against installed Hermes if required. Current evidence: `write_hermes_plugin_installs_plugin_and_enables_config`, `hermes_plugin_smoke_rewrites_and_passes_through_empty_stdout`, `patch_hermes_config_preserves_existing_enabled_plugins`, `test_setup_hermes_writes_instructions_plugin_and_config`. |
| Rules-only agents | Windsurf, Cline/Roo, Codex, Kilo, Antigravity | `README.md` and `hooks/README.md` classify these as rules/instructions-level guidance except Codex AGENTS.md. | Packet28 setup now exposes Codex, Windsurf, Cline, Roo Code, Kilo Code, and Antigravity instruction targets; instruction-only doctor verifies the guidance file and states no transparent rewrite is claimed. Windsurf is MCP/rules verified and command rewrite guidance-only: setup preserves existing MCP servers and hook entries, generated MCP config completes initialize/tools-list, and doctor reports `windsurf_rewrite_support` honestly. | partial | Add live smoke evidence that each target runtime reads the generated file. Current evidence: `test_setup_windsurf_writes_rules_hooks_and_mcp`, `test_setup_windsurf_preserves_existing_mcp_servers_and_hooks`, `test_windsurf_generated_mcp_config_smoke_test`, `test_windsurf_doctor_passes_with_generated_mcp_config`, `test_setup_cline_writes_instruction_only_rules_without_mcp_or_hooks`, `detect_runtimes_includes_instruction_only_parity_targets`. |
| Reducer output and raw recovery | filters plus tee hints | `src/core/tee.rs`, `src/core/README.md`, command modules, and parser tiers preserve raw recovery hints. | Packet28 reducer packets and artifact/fetch paths exist; `Packet28 run` records raw/reduced estimates and exposes a fetchable raw artifact handle for reducer-aware, fallback, and failing reduced runs across git, fs, rust, JavaScript, Python, Go, Ruby, .NET, infra, and GitHub families while preserving child exit codes and raw stderr. | partial | Broaden compact-output quality assertions across every reducer family. Current evidence: `test_run_reduced_command_exposes_fetchable_raw_artifact`, `test_run_raw_artifact_available_across_reducer_families`, `test_run_fallback_command_exposes_fetchable_raw_artifact`, `test_run_failing_reduced_command_preserves_exit_and_raw_stderr`, `test_run_reduces_ruby_and_dotnet_commands`. |
| Savings analytics | `rtk gain`, `rtk cc-economics` | `src/analytics/gain.rs`, `src/core/tracking.rs`; README documents graph/history/daily/all/json/csv/failures/quota/reset and Clap defines short aliases including `-g`, `-H`, `-q`, `-d`, `-w`, `-m`, `-a`, `-f`, `-F`, and `-t`. `src/analytics/cc_economics.rs` and `src/analytics/ccusage.rs` merge Claude Code `ccusage` spend with RTK saved-token history using weighted input-token economics and graceful ccusage degradation. | `Packet28 gain` summarizes local run savings, route counts, token estimates, JSON output, CSV export via `--format csv`, per-run history via `--format history`/`--history`/`-H`, daily/weekly/monthly buckets via `--format daily|weekly|monthly`, `--daily|--weekly|--monthly`, or `-d|-w|-m`, quota impact via `--format quota`, `--quota`, or `-q --tier/-t pro|5x|20x` with `--quota-tokens` as an explicit override, route graph output via `--format graph`, `--graph`, or `-g`, combined sections via `--format all`, `--all`, or `-a`, failed/fallback runs via `--format failures`, `--failures`, or `-F`, and non-interactive reset via `--reset --yes`. `Packet28 cc-economics` merges recorded Packet28 run savings with ccusage-style daily/weekly/monthly JSON, computes weighted input-token cost per token and estimated savings, supports text/JSON/CSV output, and degrades to local Packet28 savings if ccusage is unavailable. | partial | Broaden provider/session sources and richer RTK-style visual graph output if needed. Current evidence: `test_run_reduces_git_status`, `test_gain_reports_failed_and_fallback_runs`, `test_cc_economics_merges_ccusage_and_packet28_savings`. |
| Missed savings discovery | `rtk discover` | `src/discover/*`; README documents current project/all/since/limit/json flows and command-chain classification. | `Packet28 discover` scans local run savings plus Claude-style JSONL sessions, splits chained Bash commands, supports `--all`, `--since <days>`, `--limit`, and JSON output, and reports supported/unsupported command counts. | partial | Add broader provider breadth and richer missed-savings recommendations. Current evidence: `test_discover_reports_run_missed_savings`, `test_discover_splits_chained_session_commands`, `test_discover_all_and_since_scan_multiple_session_files`. |
| CLI correction learning | `rtk learn` | `src/learn/mod.rs`, `src/learn/detector.rs`, and `src/learn/report.rs` scan Claude Code sessions for fail-then-succeed command pairs, classify error types, filter by confidence/occurrence thresholds, emit JSON/text reports, and optionally write `.claude/rules/cli-corrections.md`. | `Packet28 learn --sessions-dir <dir>` scans Claude-style session JSONL in tool-use/tool-result order, detects fail-then-succeed command corrections with matching base commands, classifies common error types, supports `--all`, `--project`, `--since`, `--limit`, `--min-frequency`, `--min-confidence`, `--format json`, and `--write-rules`, and writes `.claude/rules/cli-corrections.md` locally. | partial | Add richer semantic grouping if needed. Current evidence: `test_learn_detects_cli_correction_from_session_history`. |
| Session analytics | `rtk session` | `src/analytics/session_cmd.rs` analyzes adoption across recent Claude Code sessions, skips subagent files, splits chained shell commands, prints a session/date/command/adoption/output table with a compact progress bar, and reports average adoption. | `Packet28 session` reports Packet28 task registry state by default and `Packet28 session --sessions-dir <dir>` scans Claude-style JSONL for Packet28-covered command adoption with `--limit`, `--all`, `--since <days>`, JSON output, and RTK-style text output with session id, relative date, command count, Packet28-covered count, adoption percent, progress bar, output-token estimate, average adoption, and discover hint. | partial | Add broader provider coverage if needed. Current evidence: `test_session_reports_adoption_from_session_jsonl`, `test_session_adoption_all_and_since_scan_multiple_session_files`. |
| Custom config and filters | `~/.config/rtk/config.toml`, `src/filters`, `exclude_commands`, `.rtk/filters.toml`, `~/.config/rtk/filters.toml`, `rtk verify`, `rtk trust` | `src/core/config.rs`, `src/core/toml_filter.rs`, `src/filters/README.md`, `src/hooks/verify_cmd.rs`, trust-gated project filters, and `hooks/README.md` document custom filters, exclude patterns, opt-in behavior, subcommand excludes, `^` regex excludes, a TOML output-filter pipeline, inline filter verification, and explicit trust before project-local filters run. | Packet28 reads repo-local `covy.toml`, `packet28.toml`, or `.packet28.toml`, user-global `~/.config/packet28/config.toml`, and RTK-compatible `~/.config/rtk/config.toml`; CLI, MCP, and hook rewrite paths honor `[hooks]`/`[rewrite]` excludes including subcommand and `^` regex patterns with `config_excluded`, and preserve configured transparent wrappers such as `poetry run`. `Packet28 run` raw-fallback commands load first-match TOML output filters from trusted project `.packet28/filters.toml`/`.rtk/filters.toml`, user-global `~/.config/packet28/filters.toml`/`~/.config/rtk/filters.toml`, and the embedded RTK-compatible built-in filter catalog, preserving raw artifacts while applying RTK-style `match_command`, `strip_ansi`, `replace`, `match_output` with `unless`, strip/keep line filters, truncation, head/tail/max lines, `on_empty`, and `filter_stderr`; transparent rewrite planning routes matching TOML-filter commands to `Packet28 run` when no reducer/proxy route claims them first. `Packet28 verify filters` runs inline `[[tests.<filter>]]` cases for project, global, and built-in filters, supports `--filter`, `--require-all`, JSON output for CI, and `--trust` to approve the full project filter file after successful verification. | implemented | Current evidence: `route_registry::tests`, `rtk_global_config_excludes_subcommand_patterns_from_rewrite`, `rtk_global_config_excludes_regex_patterns_from_rewrite`, `test_top_level_rewrite_respects_repo_exclude_config`, `toml_filters::tests::*`, `test_run_applies_project_toml_filter_to_fallback_command`, `test_run_skips_untrusted_project_toml_filter`, `test_run_applies_builtin_rtk_compatible_toml_filter`, `test_verify_filters_runs_inline_toml_tests`. |
| Telemetry controls | `rtk telemetry enable/status/disable/forget` | `README.md`, `docs/TELEMETRY.md`, `src/core/telemetry.rs` document opt-in anonymous telemetry. | Packet28 is local-first with no telemetry. | non-goal | Keep privacy docs explicit; do not add telemetry unless product direction changes. |
| Graceful hook degradation | hooks exit 0 on missing binary, bad JSON, old version, rewrite failure | `hooks/README.md` defines exit-code contract and graceful degradation. | Packet28 generated command hooks wrap the Packet28 binary in a POSIX shell guard that exits 0 when the binary is missing across Claude, Cursor, Copilot, Gemini, and Windsurf command-hook configs; OpenCode and Hermes plugins check for Packet28 before registering or rewriting and fail open on rewrite errors/timeouts. Hook entrypoints log and allow malformed JSON or processing errors, and no-rewrite PreToolUse paths exit successfully without mutating runtime input. | implemented | Current evidence: `generated_packet28_hook_command_exits_zero_when_binary_is_missing` covers Claude/Cursor/Copilot/Gemini/Windsurf generated commands; `test_packet28_hooks_degrade_gracefully_on_bad_json_and_no_rewrite` covers malformed JSON for Claude/Cursor/Copilot/Gemini plus no-rewrite pass-through. |

## ICM Parity Matrix

| ICM capability | ICM command/API/tool | Upstream evidence | Packet28 equivalent | Status | Test or evidence needed |
|---|---|---|---|---|---|
| Local SQLite memory | `icm --db`, default store | `crates/icm-store/src/schema.rs` creates durable SQLite tables for memories, memoirs, concepts, feedback, transcripts, pending extractions, hook events, and metadata. | `~/.packet28/packet28.db` via `memory_store.rs` with events, commands, reductions, memories, memory_chunks, memory_embeddings, concepts, memoirs, relations, feedback, transcript_sessions, transcript_messages, agent_sessions, mcp_calls, hook_events, pending_extractions, `memories_fts`, `concepts_fts`, `feedback_fts_all`, and `transcript_messages_fts`; legacy databases are migrated in place with rich metadata columns and rebuilt external-content FTS tables. | implemented | Current schema/migration/FTS/hook/queue evidence: `test_memory_store_migrates_legacy_sqlite_schema`, `test_memory_store_recall_uses_sqlite_home_db`, `test_memory_pending_extraction_queue_processes_into_memory`, `test_feedback_and_graph_cli_use_sqlite`, `test_hook_records_local_event_log_stats_and_dashboard_count`. |
| Memory store | `icm store --topic --content --importance --keywords --raw` and `icm_memory_store` | `crates/icm-cli/src/main.rs`, `crates/icm-mcp/src/tools.rs`. | `Packet28 memory store <content> --tags --topic --importance --keywords --project --source --raw`; MCP `packet28.memory_store` accepts the same metadata; importance is normalized and validated to `low`, `medium`, `high`, or `critical`, and initializes memory weight on Packet28's local `0..1` scoring scale. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_memory_recall_scores_importance_and_keywords`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Memory recall | `icm recall <query> --topic --limit --keyword --project --format` | ICM recall supports filters, compact/detail/json output, FTS/vector hybrid in store. | `Packet28 memory recall <query> --limit --topic --project --tag --keyword --format simple|toon|detail|json`; MCP `packet28.memory_recall` supports the same filters. Current store merges FTS5 candidates with local deterministic vector candidates when embeddings exist, dedupes by memory id, exposes a combined `recall_score`, boosts importance/weight/keyword/tag matches, and falls back to scored LIKE only when hybrid recall returns no records. Local embeddings use the `packet28-local-lexical-v2` model, which hashes normalized tokens, token parts, character n-grams, and memory metadata into a normalized local vector while preserving read compatibility with `packet28-local-hash-v1` rows. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db` covers CLI recall formats, embedding creation, and typo-tolerant vector recall; `test_memory_recall_scores_importance_and_keywords`; `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Memory list | `icm list --topic --all --sort` | CLI list command. | `Packet28 memory list --topic --project --all --sort recent|oldest|importance|weight`; MCP `packet28.memory_list` supports the same list controls. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Forget memory/topic | `icm forget [id] --topic`, `icm_memory_forget`, `icm_memory_forget_topic` | CLI and MCP tools. | `Packet28 memory forget <id>` and `Packet28 memory forget --topic <topic>`; MCP `packet28.memory_forget`. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Update memory | `icm update <id> --content --importance --keywords`, `icm_memory_update` | CLI and MCP tools. | `Packet28 memory update <id> --content --tags --topic --importance --keywords --source --raw`; MCP `packet28.memory_update`; importance validation matches store semantics and raises the stored weight floor when importance is upgraded. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_memory_recall_scores_importance_and_keywords`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Memory health/topics/stats | `icm health`, `icm topics`, `icm stats`, MCP stats/topics tools | CLI and MCP tools. | `Packet28 memory topics`, `Packet28 memory stats`, `Packet28 memory health`; MCP `packet28.memory_topics`, `packet28.memory_stats`, `packet28.memory_health`. Health reports topic counts, average weight, average access count, stale-memory counts, age, and consolidation signals. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Decay/prune | `icm decay`, `icm prune --dry-run` | CLI commands and README decay model state critical memories never fade, high-importance memories decay slower, low-importance memories decay faster, frequently recalled memories decay slower, and high/critical memories are not auto-pruned. | `Packet28 memory decay --factor <n>` and `Packet28 memory prune --threshold <n> [--dry-run]`; MCP `packet28.memory_decay` and `packet28.memory_prune`. Recall updates local access counters, critical memories are skipped by decay, high memories decay at half speed, low memories decay at double speed, frequently recalled memories decay slower, and critical/high memories are protected from prune with `skipped_protected_count` reported. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Consolidation | `icm consolidate --topic --keep-originals --summarizer-*`, `icm_memory_consolidate` | CLI and MCP support deterministic or summarizer-backed consolidation. | `Packet28 memory consolidate --topic <topic> [--keep-originals]` and MCP `packet28.memory_consolidate` create a deterministic local consolidated summary memory, preserve merged tags/keywords/project/source/raw metadata, carry forward highest importance, and optionally delete source memories. Remote/API summarizer-backed consolidation is a Packet28 non-goal unless a local-first summarizer backend is added. | implemented | Current evidence: `test_memory_consolidate_preserves_metadata_and_deletes_sources`, `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Embeddings/vector search | `icm embed`, optional fastembed, `vec_memories` | `crates/icm-store/src/schema.rs`, `fastembed_embedder.rs`. | `Packet28 memory embed` and MCP `packet28.memory_embed` create local deterministic `packet28-local-lexical-v2` embeddings in `memory_embeddings` without API keys; recall uses those embeddings for local vector fallback with `recall_score`, including typo-tolerant lexical matches and metadata-aware vectors. Packet28 keeps legacy `packet28-local-hash-v1` rows readable but writes the stronger lexical model by default. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| FTS search | `memories_fts`, `feedback_fts`, `concepts_fts` | ICM creates FTS5 virtual tables and triggers. | Packet28 now creates `memories_fts`, `feedback_fts_all`, `concepts_fts`, and `transcript_messages_fts` with triggers, blends memory FTS/vector candidates when embeddings exist, and then LIKE fallback. Memory recall scoring calibrates FTS/BM25 rank with deterministic importance, weight, metadata, content-term, raw-excerpt, and exact-phrase bonuses before returning `recall_score`; feedback, concept, and transcript searches use FTS first and then LIKE fallback. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_memory_recall_scores_importance_and_keywords` covers phrase-ranking calibration, `test_feedback_and_graph_cli_use_sqlite`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Memoir graph | memoir create/list/show/delete/add/refine/link/search/export/stats/distill | `crates/icm-cli/src/main.rs`, `crates/icm-core/src/memoir*`, `crates/icm-store/src/schema.rs`. | `Packet28 graph create/list/show/delete/add-concept/refine/link/search/export/stats/inspect/inspect-concept/distill`; concepts carry memoir container, labels, confidence, revision, source ids, and timestamps; graph search filters by memoir and label; concept inspection returns a depth-limited neighborhood. Distillation is local-first: topic memories become primary labeled concepts with memory source ids, additional memory keywords become related concepts in the same memoir, and Packet28 links the primary concept to related keyword concepts with `mentions` relations instead of calling a remote LLM. MCP exposes graph create/list/show plus concept/link/search/export/stats/delete/inspect/distill tools. | implemented | Current evidence: `test_feedback_and_graph_cli_use_sqlite` covers CLI memoir metadata, concept inspection, distillation, related keyword concepts, and `mentions` relations; `test_mcp_memory_store_recall_uses_sqlite_home_db` covers MCP graph distillation. |
| Learn project graph | `icm learn`, `icm_learn` | CLI and MCP scan project into memoir graph. | `Packet28 learn --project-dir <dir> --project-name <name> --memoir <memoir>` and MCP `packet28.learn_project` scan local manifests, dependencies, modules, entrypoints, and configs into a selected Packet28 memoir graph with typed relations; learned concepts use the Packet28 graph metadata schema. | implemented | Current evidence: `test_feedback_and_graph_cli_use_sqlite`, `test_mcp_memory_store_recall_uses_sqlite_home_db`. |
| Feedback correction loop | `icm feedback record/search/list/delete/stats`, `icm_feedback_*` | CLI and MCP; schema includes topic, context, predicted, corrected, reason, source, applied_count, FTS. | `Packet28 feedback record/search/list/apply/delete/stats` with topic, context, predicted, correction, reason, source, applied count, and FTS-backed search; MCP exposes matching `packet28.feedback_*` tools. | implemented | Current evidence: `test_feedback_and_graph_cli_use_sqlite`, `test_mcp_memory_store_recall_uses_sqlite_home_db`, `cmd_mcp::tests::tools_list_exposes_product_compatibility_aliases`. |
| Transcript storage/search | `icm transcript ...` | `icm-core/src/transcript*.rs`, schema sessions/messages and transcript stats/search. | `Packet28 transcript append/list/show/search/stats/export/import` store session/message rows with FTS-backed search and a local JSON export/import format; MCP exposes `packet28.transcript_append/list/show/search/stats/export/import`; runtime hooks append successful `PostToolUse` output and failed `PostToolUseFailure` diagnostics into searchable transcript messages with session, agent, source, and project metadata. | implemented | Current evidence: `test_feedback_and_graph_cli_use_sqlite`, `test_transcript_export_import_round_trip`, `test_hook_records_local_event_log_stats_and_dashboard_count`, `test_hook_failure_output_is_searchable_transcript_context`, `test_mcp_memory_store_recall_uses_sqlite_home_db`, `cmd_mcp::tests::tools_list_exposes_product_compatibility_aliases`. |
| Wake-up packs | `icm wake-up`, `icm_wake_up` | `icm-core/src/wake_up.rs`, CLI/MCP support project and compact context packs. | `Packet28 wakeup --project <name> --max-tokens <n> --format markdown|plain` and MCP `packet28.wakeup` summarize local memory, feedback, transcripts, graph, and stats, with project filtering applied to durable memories, feedback corrections, and transcript messages. Feedback/transcript records carry optional project metadata through CLI, MCP, hook capture, transcript export/import, and wake-up candidate rendering. Wake-up output includes a deterministic budgeted `pack`, item scores, estimated tokens, and truncation state while preserving raw component arrays. Claude SessionStart hooks inject the same budgeted wake-up pack when matching local memory exists, merged with Packet28 handoff context if present; non-Claude runtimes remain pre/post-tool rewrite/capture adapters because they do not expose a compatible SessionStart context-injection contract in Packet28 today. | implemented | Current evidence: `test_memory_store_recall_uses_sqlite_home_db`, `test_mcp_memory_store_recall_uses_sqlite_home_db`, `test_feedback_and_graph_cli_use_sqlite`, `test_packet28_hook_session_start_injects_wakeup_pack` covers SessionStart injection plus budget truncation. |
| Hook extraction lifecycle | Claude SessionStart / SessionEnd, pending extraction, hook-log, hook-stats | ICM changelog/schema/CLI mention hook events, pending extractions, hook-log/stats. | Packet28 runtime hooks capture routine activity, persist local hook_events with `Packet28 hook log`/`Packet28 hook stats`, inject budgeted wake-up context on Claude SessionStart, persist Claude SessionEnd lifecycle events, and best-effort enqueue PostToolUse/PostToolUseFailure output into the local pending extraction queue and transcript store. CLI and MCP expose pending queue list/process/delete/stats, and dashboard reports pending counts. Extraction is deterministic/local-first rather than remote LLM-backed. | implemented | Current evidence: `test_hook_records_local_event_log_stats_and_dashboard_count`, `test_hook_failure_output_is_searchable_transcript_context`, `test_hook_session_end_is_recorded_in_local_lifecycle_log`, `test_memory_pending_extraction_queue_processes_into_memory`, `test_mcp_memory_store_recall_uses_sqlite_home_db`, `test_packet28_hook_session_start_injects_wakeup_pack`. |
| MCP tool surface | `icm_memory_*`, `icm_memoir_*`, `icm_feedback_*`, `icm_transcript_*`, `icm_wake_up`, `icm_learn`, `icm_embed` | `crates/icm-mcp/src/tools.rs` exposes broad memory/memoir/feedback/transcript/wake-up tools including memory store/recall/update/forget/consolidate/embed/extract-patterns, memoir create/list/show/add/refine/link/search/search-all/inspect/export, feedback record/search/stats, transcript start/record/search/show/stats, wake-up, and project learn. | Packet28 MCP exposes search/reduce/rewrite/memory/embed/extract-patterns/feedback/transcript/wakeup/learn/graph/handoff/doctor tools. The Packet28-native ICM-equivalent surface covers memory store/recall/list/update/forget/topic/stats/health/decay/prune/consolidate/embed/extract-patterns/pending extraction, graph memoir create/list/show/delete/add/refine/link/search/export/stats/inspect/distill, feedback record/search/list/apply/delete/stats, transcript append/list/show/search/stats/export/import, wake-up packs, and project learn. Pattern extraction is deterministic/local-first and can optionally materialize recurring memory patterns as graph concepts. | implemented | Current evidence: `cmd_mcp::tests::tools_list_exposes_product_compatibility_aliases`, `test_mcp_memory_store_recall_uses_sqlite_home_db` covers MCP memory extraction, graph concept materialization, graph, feedback, transcript, wake-up, learn, decay/prune, and pending extraction; `test_memory_consolidate_preserves_metadata_and_deletes_sources` covers CLI pattern extraction. |
| Dashboard/TUI/web | `icm dashboard`, `icm tui`, optional web feature | `crates/icm-cli/src/tui.rs`, `crates/icm-cli/src/web.rs`, `crates/icm-cli/web/src/routes/+page.svelte`, and the changelog document an interactive TUI plus an optional Svelte web dashboard with memory/topic/graph/feedback stats. | `Packet28 dashboard` local CLI/JSON report includes savings, missed savings, sessions, recent memories, memory topics, memory health, graph stats/relation types, feedback stats, transcript stats, MCP calls, hook event history, pending extraction count, and integration health. `Packet28 dashboard --format html --output <path>` renders a local static HTML dashboard with the same core metrics, memory topics, noisy commands, and integration health without starting a server or sending data off-machine. `Packet28 dashboard --format tui` renders a local terminal dashboard, and `--interactive` accepts navigation commands for Overview, Memory, Graph, Feedback, and Integrations panels. | implemented | Current evidence: `test_dashboard_shows_local_product_metrics` covers JSON/text/HTML/TUI/interactive TUI output; `test_hook_records_local_event_log_stats_and_dashboard_count`; `test_feedback_and_graph_cli_use_sqlite`. |
| Cloud/import/upgrade flows | import/upgrade/cloud modules | ICM has import, upgrade, and cloud-related CLI modules. | Packet28 is local-first. | non-goal | Keep local-first privacy policy explicit unless product direction changes. |

## Evidence Requirements Before Claiming Full Parity

Packet28 must not claim full RTK + ICM parity until:

1. Every `partial` or `missing` row above is changed to `implemented` or explicitly accepted as `non-goal`.
2. Each `implemented` row names a concrete test, smoke, or experiment artifact.
3. Runtime hook claims are proven with generated config plus initialize/tools-list or hook-payload smoke tests.
4. Reducer claims prove exit-code preservation, raw artifact recovery, compact output, fallback reasons, and critical-error visibility.
5. Memory claims prove storage, recall, FTS behavior, graph inspection, feedback search, wake-up rendering, and MCP access.
6. Real-world experiment artifacts under `docs/experiments/` include repeated runs across multiple repositories and workflows.
7. Release smoke verifies the published install path after CI passes.
