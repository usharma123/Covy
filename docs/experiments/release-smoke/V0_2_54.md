# Packet28 Release Smoke - v0.2.54

This artifact records the published-install evidence for the audited RTK `55f998d`
and ICM `92bd851` parity claim. It is release evidence, not a claim that later
workspace versions have been published.

## Commands

```bash
cargo test --workspace
npm view packet28@0.2.54 version
npm install -g packet28@0.2.54
packet28 --version
packet28 rewrite "git status --short"
packet28 setup --runtime gemini --root <isolated-root> --yes
packet28 doctor --agent gemini --root <isolated-root> --json
```

## External CI And Release Records

- GitHub Hook Benchmark Suite run: `25753409843`
- GitHub Release workflow run: `25753420426`
- GitHub release: `v0.2.54`
- npm package check: `npm view packet28@0.2.54 version` returned `0.2.54`

## Smoke Result

- Local workspace tests passed before release: yes
- GitHub release workflow passed: yes
- Published npm version matched the release tag: yes
- Isolated global install smoke passed: yes
- Runtime setup smoke included Gemini setup and doctor checks: yes

The isolated install smoke verified `packet28 --version`, setup runtime coverage,
rewrite behavior, `setup --runtime gemini`, and `doctor --agent gemini --json`.
